# Chintan backlog

The running ledger of what has been asked of Chintan and what was decided: the owner's trial notes, the QA pass's findings and the external reviews' items, from 2026-09-04 on, in dated sections. Completed rows stay, so the file is the record of why things are the way they are as much as the queue of what is next. One line per item. Status is one of **fixing** (in a branch now), **next** (agreed, not started), **design** (needs a decision; the proposal is in the note), **answer** (a question, answered in the note), **question** (waiting on the owner), **declined for now** (deliberately not built; the note says why and when to revisit), **dropped** (the owner withdrew it), **done**.

## Bugs

| # | Item | Status | Notes |
|---|---|---|---|
| B1 | Transcript panel says "made before timestamps were captured" for every recording; Copy transcript copies nothing | **done** | Root cause found: `segments.json` uses `start_ms`/`end_ms`, the parser expected `start`/`end` in seconds, so every segment was dropped. Backend was fine (`has_segments: true`, file present). Copy also gets a touch-safe fallback and clearer scope ("Copy this transcript" = that recording). |
| B2 | Note shows old text after a recording is filed into it; needs a second open or a reload | **done** | No cache invalidation when a capture reaches `appended`. The filing poll now invalidates the note, the list and the offline corpus on that transition, and on "Open the note". |
| B3 | Filing row should disappear once "Open the note" or Dismiss is tapped, and must not come back | **done** | Dismissed ids move from an in-memory set to `localStorage`; the 10-minute auto-hide is removed (rows stay until you act). |
| B4 | Previous recording's waveform flashes when a new recording starts | **done** | Two causes. First fix: the recorder kept the old session until `getUserMedia` resolved. What remained (N6): the canvas draws its first frame from the child's effect, before the screen's own effect has armed the next recording, so it read the old collector — and its draw effect does not re-run until the stream is live, so that frame stayed on the element for the whole microphone request. |
| B5 | Search does not find words inside transcripts | **done** | Both server and local search only saw title/aliases/tags/snippet. The note index gains `search_text` (lowercased body, ≤32 KB), searched on the server and shipped to the offline corpus; one-time backfill for existing notes. |
| B6 | Version string shows `v0.5.0-14-gabcdef` | **done** | Two parts: deploys now tag `main` (`vX.Y.Z+1`) and publish a release; the footnote shows the tag, or `v0.5.0+14 (abcdef)` when between tags. |
| B7 | Passkey setup is never offered | **done** (hand-off) | Confirmed in a browser: Cognito's managed login does not prompt for passkey registration after a password sign-in; it only offers "sign in with passkey" to users who already have one. In-app registration was tried against prod (2026-09-04) and is impossible: `StartWebAuthnRegistration` returns `rp.id` = the managed-login domain, and `navigator.credentials.create` from `vppillai.github.io` fails with `SecurityError: The relying party ID is not a registrable domain suffix of, nor equal to the current domain`; the hosted-UI access token also lacks `aws.cognito.signin.user.admin`, so even listing credentials is refused. What works is the managed login's own page, `/passkeys/add?client_id=…&redirect_uri=…`, which needs a live managed-login session and returns to the app with `?result=success` or `?result=invalid_session`. Shipped: a "Sign in with a passkey" card on **You** that hands off to that page and reports the result, a one-time nudge on the library (dismissal remembered per device), and unsupported-browser copy. Listing/removing passkeys in-app would need the client's OAuth scopes to include `aws.cognito.signin.user.admin` (infra). |
| B8 | Upload feels slow even for tiny recordings | **done** | Measured in the 2026-09-04 log review (§3): the server's `POST /v1/captures` is 21 ms p50 and never cold; the client's audio PUT is 1–2 s on a good network and uncorrelated with size; the pipeline takes 2–9 s; and the 4-second poll added 0–4 s before the row read Filed. What shipped (#28): the capture poll runs every 1.5 s for a pending capture's first 30 s and 4 s after (`CAPTURE_POLL_FAST_MS`), and uploads are bounded. The remaining seconds are the providers' (transcribe, route, cleanup). |
| B9 | Filing sometimes takes ~1 minute | **done** | One routing call to MiniMax took 63 s; others take 1–2 s. The log review checks whether this is provider variance or our prompt/settings and proposes a timeout + fallback. |
| B10 | "Add a recording to this note" → after Stop the filed text isn't in the note until re-entering | **done** | Same cause as B2. |
| B11 | Manual edit: how many API calls? | **answer** | One PATCH per pause in typing (autosave debounces; since yesterday saves are serialised, one in flight at a time, and the version comes from the response). The QA pass counts requests for a typed sentence; if it is more than ~1 per pause, we widen the debounce. |
| B12 | Recording continues when the phone screen locks? | **design** | Android Chrome keeps a `MediaRecorder` alive with the screen locked as long as the tab holds the mic (it shows a notification); iOS Safari suspends it. The app already holds a wake lock, which keeps the screen ON instead. Proposal: keep the wake lock (safer for eyes-off use), and add an explicit "screen may lock" mode later only if Android testing shows recording survives it. Needs a real-device test. |
| B13 | Incoming call while recording | **done** | The track `mute`/`ended` handlers exist; make `mute` auto-pause (not just flag) and show "Paused: microphone taken by another app" live, resume on `unmute`. |
| B14 | PWA shortcut / launch latency | **next** | The "Record a thought" shortcut ships in the manifest (→ `/capture`). Launch latency was measured by QA (cold load of `/` and `/capture` on Fast 3G, 4× CPU) but none of the candidates has landed as of v0.5.9: the bundle is one 534 KB chunk (170 KB gzip), the capture route is not precached and it still fetches notes/tags/archived. Those three remain the work; the preconnect to the API and Cognito has landed (`vite.config.ts` injects both hints and the live index carries them). |
| B15 | A recording made into a note keeps the spoken filing instruction ("Create a note with the title …") in the body, while a routed one has it removed | **done** | Live QA 2026-09-05 §5a. The targeted path skipped routing and with it the removal of instruction spans. It now makes the same span-extraction call as routing when the transcript contains an instruction cue ("add this to", "create a note", "title this", … — `routing.MentionsInstruction`), with the target note as the only candidate, removes the spans and ignores the destination; no cue, no call. Priced as `route` through the breaker; a failed call keeps the words as spoken. |
| B16 | The router created a second note with the title of an existing active note | **done** | Live QA 2026-09-05 §5b. Q10's rule — an existing note with the same title is the destination — was only in the prompt. It now holds in code after the model answers: a `new` decision whose title matches an active candidate's title or alias (case- and whitespace-insensitive) appends to that note with confidence 1 and logs the note id. |
| B17 | An Ask answer saved as a note is cited as a source by later questions | **done** | Live QA 2026-09-05 §4. Notes tagged `ask` — the tag the app puts on a saved thread — are left out of Ask retrieval (`ask.Retrievable`), so an answer never cites an earlier answer. The frontend sets the tag when it saves. |

## UX / product

| # | Item | Status | Notes |
|---|---|---|---|
| U1 | Pull-down to refresh | **done** | Library and note screen; native-feel with `overscroll-behavior` guard. |
| U2 | "Select" button top-right looks odd; long-press to select on phones; multi-select easy on desktop; bulk actions not at the end of the list | **done** | Proposal: remove the header button. Phone: long-press a row enters selection (checkbox column slides in, haptic tick); desktop: hover reveals a checkbox at the row's left edge, click selects, shift-click ranges. Bulk bar becomes a **sticky bar above the tab bar** (not the list end) with count · Archive · Delete forever · Cancel. |
| U3 | Pagination / page-size setting for long lists | **done** | Proposal: infinite scroll ("Load more" already exists; make it automatic on scroll) rather than page-size settings — settings for page size are a desktop idiom. Day groups already chunk the list visually. Open to overriding. |
| U4 | Record button larger, never covering the last row | **done** | Bigger centre button (64→76 px) and bottom padding on the list equal to the bar height plus safe area, so the last row is always fully visible above it. |
| U5 | Home title "Notes" is plain | **done** | Proposal: a greeting-free header with the app wordmark on the left and the day's date in serif on the right ("Thursday 4 Sept"), search below. Alternative: keep "Notes" but as a serif display with the count ("Notes · 12"). |
| U6 | Tab label "Notes" → "Home" | **done** | |
| U7 | Word count of the note | **done** | In the meta line after recordings: "412 words". |
| U8 | About page + GitHub link | **done** | On You: "About Chintan" → what it does, how filing works, privacy (where data lives), version, link to the repository. |
| U9 | Icons and app icon polish | **done** (stroke pass; app icon PNG still to redraw) | Keep a single stroke-weight icon set (e.g. Lucide-style 1.75 px strokes, tuned) and redraw the app icon around the wordmark's serif "C" with the accent dot; no emoji anywhere. I'll propose an icon sheet as an artifact before applying. |
| U10 | Don't wait on the send screen; hand off to the background as soon as Send is tapped | **done** | Agreed: Send → back to the library immediately, the filing row shows upload → transcribe → file. The upload already survives navigation (it lives in the store); only the screen's waiting behaviour changes. |
| U11 | Playback of the just-recorded audio before Send, with scrubbing | **done** | The buffered chunks are on disk; assemble a Blob and reuse the note player + scrubber on the review screen. |
| U12 | Horizontal scroll through the full waveform while uploading | **done** | Aesthetic; pairs with U11 (same waveform component). |
| U13 | Spending-cap text in Settings without a control | **done** | Read-only since 2026-09-03; per-user usage accounting is being added on the backend (see D6). The line will show your usage this month instead of a cap once `GET /v1/usage` exists. |
| U14 | Dismiss semantics of filing rows | **done** | See B3. |

## Features needing design

| # | Item | Status | Proposal |
|---|---|---|---|
| D1 | Advanced cleanup ("smart") vs the current misheard-word cleanup, with raw and cleaned as tabs; re-run over the whole note when new audio arrives | **done** | Backend in #36, the Cleaned tab (Structured / Polished, "Keep it updated after each recording", Generate) in #37. Per-recording cleanup stays as it is (`faithful`/`polished` per capture). The note gains one **cleaned view** of the entire body — `structured` (Markdown: short headings, lists for enumerations, filler and repetition removed) or `polished` (prose only, light touch) — generated by the worker's `clean-note` task from the body with the markers stripped, one LLM call through the breaker (`op: clean_note`, input capped at 150 KB, output at 200 KB, refused whole rather than truncated), stored on the note row (`cleaned_body`, promoted like `search_text`). `POST /v1/notes/{id}/clean` (optional `{mode}`) answers 202 and invokes the worker; `PATCH` takes `auto_clean` and `cleaned_mode`; `NoteDetail.cleaned` is `null` or `{body, mode, generated_at, stale, error?}`. Every body write — editor save, append, move, delete — marks an existing view **stale**; with `auto_clean` on, an append (the worker invokes itself), a move or a delete (the API invokes the worker) regenerates it; an editor save never does. The view is **read-only**: edits belong in the body, and the view is regenerated from it. A failed run keeps the previous view and stores a fixed `cleaned.error`. Both exports carry the view (`note.cleaned` in the JSON export, `<slug>-<id>.cleaned.md` from `chintanctl export`). `search_text` stays derived from the raw body. |
| D2 | Move a misfiled recording to another note; splice text by timestamp | **done** | Recording rows get **Move to…** (single) and multi-select on recordings. Moving = remove that capture's marker-delimited paragraph from note A (we have exact markers since yesterday), append it to note B at the position implied by the recording's time relative to B's recordings (chronological insert, not always at the end), re-point the capture row. The transcript is not re-run. Structured mode (D1) re-runs on both notes. |
| D3 | Delete a single recording and its text | **done** | Same primitive as D2's removal step: delete the marker-delimited paragraph, delete the capture row and objects (audio, raw, clean, segments, peaks), typed confirmation. |
| D4 | Download all recordings of a note as one archive; multi-select recordings | **done** | Client-side zip (`fflate`, ~8 KB) from the presigned audio URLs; name files `<note>-<yyyymmdd-hhmm>.webm`; no backend change. Selection UI shared with D2/D3. |
| D5 | AI search / ask the knowledge base | **done** | Frontend landed: **Ask** as a mode of the search field (Search · Ask segment, `mode=ask` in the URL, the question never is), the Ask panel with the answer, "Not in your notes" when ungrounded, source chips that open the note, follow-ups sent with the last six turns as `history`, Save as note, Clear; the thread lives in `sessionStorage`. Backend 2026-09-05: `POST /v1/ask` (202, idempotent) + `GET /v1/ask/{askId}`; the worker's `ask` task ranks the notes lexically over title/aliases/tags/`search_text`, packs the top bodies within a 40k-rune budget, one grounded LLM call through the breaker (`ops.ask`), sources filtered to what was packed; `ASK#` rows, 24 h TTL. No per-note summary — the bodies are cheap to read for a dozen notes. Embeddings deliberately not built; see `docs/design/ask.md`. |
| D6 | Per-user usage accounting for future admin/billing | **done** | Monthly and daily `USAGE#<user>#…` counters written by the breaker (cost, calls, audio seconds, tokens) plus `GET /v1/usage`. 2026-09-05: per-provider split, `api_requests` per authenticated request, `storage` summary, the tenant's share of the AWS cost, and the `chintanctl usage` admin listing over GSI1. |
| D7 | Transcription language per note (English default, other language, or auto/mixed) | **done** | Groq Whisper takes an ISO-639-1 `language`, or auto-detects when omitted; there is no explicit mixed mode, and auto is not one: it picks one language per recording and drops or re-scripts the rest (round 3, T3), so mixed speech does best with the main language's code. Per-note `language` (`en` default, any code, or `auto`) + a `default_language` setting. UI surfaced 2026-09-05: first field of the note's **Details** panel (was hidden in Tags), named in the note's meta line (tap opens Details), and above retention on You; 28 languages with native names. |
| D8 | Offline recording; notes readable offline | **answer / next** | Recording offline works: chunks stay in IndexedDB, and since Q11 (#31, `useResendOnReconnect`) the send goes out on reconnect without a tap. Notes are cached for offline reading (the list and the search corpus); a note body is cached after its first open. Prefetching bodies so an unopened note reads offline is the half still to do. |
| D9 | Telemetry for data-driven improvement | **design** | Backend: already emits EMF metrics per stage and provider latency; add client-observed timings (record→sent, sent→filed, cold start) as one small `POST /v1/telemetry` batch per session, written to the log only (no new storage). Frontend: `web-vitals` is *not* bundled (the console errors you pasted come from a browser extension). Keep it log-only until a question needs more. |
| D10 | "Febl index 01" ring integration | **dropped** | Owner dropped it on 2026-09-05. The device and its outbound integration were never identified; had it gone ahead, the path would have been a small `POST /v1/inbox` with a per-device token. |
| D11 | Docs cleanup: remove v1/v2 history; a single-script deploy for a fresh clone | **done** | History documents removed; the 2026-09-03 review is kept as a dated report under `docs/history/`. README rewritten as: what it is · deploy · configure · operate · security · develop. `scripts/doctor.sh` checks the prerequisites and prints the next command. Verified with a fresh checkout and the scripts' dry runs. |
| D12 | Prompt-injection safety for AI features | **next** | D1/D5 add LLM calls over user content; they reuse the fenced-transcript pattern and the router's output verifier. |

## From the QA pass (docs/qa/2026-09-04/report.md)

| # | Item | Status | Notes |
|---|---|---|---|
| Q1 | Download audio never works (CORS: the `<audio>` element's no-CORS cached response poisons the later `fetch`) | **done** | `crossOrigin="anonymous"` on the element or `cache: 'no-store'` on the fetch. |
| Q2 | Re-open a note within ~30 s of editing shows the pre-edit text; next save 409s with a false "changed elsewhere" | **done** | The PATCH result is never written to the note/list query cache. Write-through on save; same root cause as Q3. |
| Q3 | Library rows stale after editing a note | **done** | See Q2. |
| Q4 | Filed receipts accumulate; dismissal not persisted | **done** (the QA sampler had misread the new `uploading` state; two real defects behind it fixed in #31) | PR #22 was meant to fix this; QA ran on that build and still saw it. Re-test, then fix. |
| Q5 | In-app URLs drop the trailing slash → outside the PWA scope; offline reload of such a URL is a browser error page | **done** | Router `basename` normalisation. Check installed-PWA behaviour on a real device after. |
| Q6 | Bulk-action bar sits below the last row (6,400 px down on a phone) | **done** | Folded into U2 (sticky bar above the tab bar). |
| Q7 | Note opened mid-filing never refreshes | **done** | Same fix family as B2/Q2. |
| Q8 | One `GET /v1/search` per keystroke | **done** | Debounce 250 ms; the local corpus stays instant. |
| Q9 | Unsaved settings silently lost on navigation | **done** | Save on change (each control), no Save button. |
| Q10 | Same speech, six runs, four outcomes, including a duplicate "Roof repair" note | **done** | Router redesign in progress; add "an existing note with the same title is the destination" rule. |
| Q11 | Offline recording waits for a manual Send after reconnect | **done** | Queue the send; see D8. |
| Q12 | Passkey enrolment never offered | **done** | See B7. |
| Q13–Q21 | Theme "Unsaved" flag, no `h1` on note, 20 px checkboxes, orphan chunk row per Cancel, tag chip outlives its notes, offline "Loading…" forever, fixed 12-row textarea, skip-link shadow, `has_peaks` on a peak-less capture (fixed server-side in #23) | **done** | Batch of small fixes. |

Performance (cold, Fast 3G + 4× CPU): library interactive ~2.95 s, `/capture` ~3.05 s; one 136 KB gzip JS chunk; four API calls on the library, one only for the "Archived · n" count.

## Added 2026-09-04 (evening)

| # | Item | Status | Notes |
|---|---|---|---|
| N1 | Long note body pushes Recordings out of reach | **done** | Note screen gets a sticky segmented control: Text · Cleaned · Recordings (N). |
| N2 | Home tab still uses the notes-list glyph | **done** | House outline in the same stroke style. |
| N3 | Recording into a note returns to the library | **done** | Send returns to that note's Recordings tab with the filing row inside it. |
| N4 | Whole-note cleaned view | **done** | `cleaned` on the note (structured or polished), regenerable, optional auto-refresh after each recording; Cleaned tab. |
| U13b | Spend-cap sentence on You is operator noise | **done** | Keep the cap (runaway guard, 100× today's spend); drop the sentence from Usage; one line on About. |
| D6b | Show AWS cost next to provider cost | **done** | Daily worker task reads the stack Budget's month-to-date actual spend (`budgets:DescribeBudget`, free) → `GET /v1/usage` → "AWS this month" line, instance-level, with an as-of date. |

## Order of work

1. B1–B6, B10 and U6 (frontend and backend branches in progress).
2. QA pass results folded in.
3. B7 passkey registration; U10/U11/U12 (send hand-off, review playback); U2/U4 selection and record button; B13 call handling.
4. D1 cleanup modes (needs your call on the three-mode proposal), D2/D3/D4 recording actions.
5. D5 Ask, D9 telemetry, D11 docs.

## Added 2026-09-05

| # | Item | Status | Notes |
|---|---|---|---|
| N5 | Live waveform much smaller than the review player's for the same recording | **done** | The live canvas drew raw RMS while `envelope()` normalised to the running peak. `PeakCollector.recent()` now divides by the same scale, with a floor (`PEAK_FLOOR`) in both so silence stays flat; one `BAR_HEIGHT_SCALE` for the live canvas, the review scrubber and the note's scrubber. |
| N6 | Previous recording's waveform still flashed for the length of the microphone request | **done** | See B4. The store retires the old session before dispatching `request`, `amplitudes()` answers only while the machine has a live recording, and the canvas is keyed by the recording's local id. |
| N7 | Recordings made into a note also appeared as filing rows on Home | **done** | Home shows only non-targeted captures (contract §3): `CaptureWire.targeted`, plus a per-device `localStorage` set of ids the uploader sent with a `noteId` for backends that predate the flag; the local upload row on Home hides when the upload is aimed at a note. The note's Recordings tab is unchanged. |
| N8 | Swipe a note or recording row for its actions on a phone | **done** | `SwipeRow` + `useSwipeActions` (pointer events, trailing tray, snap on the motion tokens, one open at a time, rubber-band, off for a fine pointer and during bulk-select). Notes: Archive · Delete in the library, Restore · Delete in the archive — delete behind the typed-title dialog, archive-then-purge from the library because the server refuses to purge an active note. Recordings: Move · Delete, opening the row menu's own sheet and dialog. |
| N9 | Long press selects text; the app "feels like a website" | **done** | Controls, rows, chips, tabs and the bars give up `user-select`, the iOS callout and the tap flash (base.css), and get a drawn `:active` state; note text, transcripts, the cleaned view and fields stay selectable. `useLongPress` cancels Android's `contextmenu` while a press is armed — now pinned by a test. |
| N10 | The UI is minimalist but a little too flat | **done** | Depth tokens: `--shadow-xs`, refined sm/md/lg, `--shadow-inset`, `--shadow-highlight`, `--gradient-lift`/`--gradient-tonal`, `--color-ground-deep`, `--color-raised-2` (Nocturne's equals raised — the contrast budget is spent there; documented). Cards on shadow-xs, lifting on hover; banner and note tab strip get a hairline and shadow; tab, selection and action bars the sheet shadow; the record button a deeper shadow and a top highlight; chips a tonal fill; filled primaries a top-edge lift; fields recessed; sheets and dialogs on raised-2 with shadow-lg. No layout or spacing change. |
| N11 | Richer per-user usage view | **done** | `GET /v1/usage` grew `providers`, `api.requests`, `storage`, per-day `api_requests` and `aws.share_micros`; You shows the split by provider under Providers, Clean note and Ask among the stages, a facts row (API requests · recordings stored with minutes and MB, approx. when capped · notes), "Your estimated share" under AWS with the Total switching to providers + share when the backend sends one (the label says which), and a dot per day for API requests on the sparkline. Every new field is optional on the frontend so it can deploy first. |
| N12 | Search inside a note | **done** | Find bar under the tab strip (lens in the header, Ctrl/⌘+F on desktop): case- and diacritics-insensitive, "3 of 12", Enter/Shift+Enter, Escape. Text tab shows a read-only mirror with marks while a query is set and returns the caret to the match; Cleaned tab marks its rendered view; Recordings says where search works. |

## Hardening 2026-09-05 (external review)

| # | Item | Status | Notes |
|---|---|---|---|
| H1 | Notes list drained only the 500 most recently CREATED notes, then ordered by touch — an old note dictated into today was missing past 500 notes | **done** | The drain reads the whole partition, bounded by a safety ceiling `repository.MaxNotesDrained` (5,000; ≈5 MB of projected rows per list at the ceiling). Hitting it sets `Page.Truncated`; the notes service logs at WARN and counts `NotesListTruncated`. The router's 500-note candidate pool is gone (the store already orders by touch). No index: the v3 decision stands; the index is the remedy if a tenant ever nears the ceiling. |
| H2 | Transcription, cleanup and clean-note had no deadline but the HTTP client's 840 s; a stall held the 900 s Lambda and then failed the capture for good | **done** | `pipeline.Config` `TranscribeTimeout` 5 min, `CleanupTimeout` 2 min, `CleanNoteTimeout` 3 min, applied inside the breaker's `Do`. A deadline is retryable: the capture stays in its stage, Lambda retries, `run` resumes at the missing artefact. `ProviderTimedOut{Stage}`. See `docs/design/pipeline-deadlines.md`. |
| H3 | A model with no price row priced at zero, so the spend cap enforced nothing and nobody knew | **done** | `meter.PriceTable.Resolve`/`Resolves`; the worker refuses to start (`log.Fatalf` naming the key and `meter.DefaultPrices`) when the STT or LLM model resolves to no row; a wildcard-priced model is counted once per process as `PriceWildcardUsed{Provider}`. Runtime fallback unchanged. |
| H4 | `DailySpendCapMicros` defaulted to 0 (no cap) | **done** | Default 5000000 ($5/day); the description names 0 as the deliberate opt-out. Both live instances pass explicit values and are unchanged. `scripts/list-instances.sh` and both instance configs no longer say absent means 0. README row for `daily_spend_cap_micros` still says `0 (count, never enforce)` — README belongs to another stream. |
| H5 | Pin `ci.yaml` like the deploy workflows | **done** | Actions on the SHAs `deploy-*.yaml` already uses, plus `actions/upload-artifact@043fb46d…` (v7.0.1). golangci-lint is fetched from its `v2.13.2` tag and told to install v2.13.2 — the release the unpinned `HEAD` script was resolving anyway, so no new findings; govulncheck `@v1.7.0`. Dependabot's `github-actions` ecosystem already covered every workflow, so the pins get bump PRs. |
| H6 | Document the tenant model | **done** | README → Security → Tenancy: one Cognito user is one tenant, what keys off that (partition, S3 prefixes, `chintanctl`, usage rows, ownership-by-key), and what changes for sharing or multi-user tenants. |
| H7 | Refactor `pipeline.go` into explicit stage objects | **next** | 1,421 lines today, but the stages are already sequential functions that each persist their artefact. Split when the next stage is added, not before: a refactor with no feature behind it is churn the `-race` suite then has to re-earn. |
| H8 | Local development path without AWS (fake auth, fake STT/LLM, memory store) | **design** | Already here: `internal/repository/memory` (store and usage rows), `internal/provider/fake` (STT, LLM, Router), `auth.WithIdentity` for tests. Missing: a verifier that accepts a dev identity, an in-process uploader and worker invoker, and an entrypoint — which cannot be `cmd/api`, because `TestProductionBinaryDoesNotLinkTestDoubles` forbids the fakes in the production import graph. So: a separate `cmd/local` (or build tag) that wires the fakes and runs the pipeline inline. |
| H9 | SQS between S3 and the worker; per-tenant spend cap | **declined for now** | Both were removed on purpose for a single-user instance. The queue: S3 invokes the worker's live alias directly and Lambda's asynchronous retry (twice, then `CaptureDLQ`) replaced the visibility-timeout redelivery whose 960 s timeout landed inside the 20-minute append lease — `infrastructure/template.yaml` (the `CaptureDLQ` comment) and `Worker.Handle`'s doc in `backend/internal/pipeline/worker.go`. The cap: `SPEND#` is instance-wide by design and the per-tenant `USAGE` rows are accounting only — "a per-tenant cap is exactly the machinery step 3 removed" (`docs/design/usage-accounting.md`, Deliberately not built). Revisit both if an instance becomes multi-user. |
| Y1 | You and About needed "massive design/UX improvements" | **done** | You: an account header (serif initial in an `--color-accent-soft` roundel, the email and sign-in time read from the id token's claims in `features/auth/identity.ts`, Sign out as a text action with the same confirm) over five hairline cards — Recording & transcription, Appearance (a three-way segmented theme control with a swatch per theme), Usage this month (month eyebrow, one large figure, Providers · AWS · Total cells, the stage table, the day strip, the fact rows), Passkeys, About & support (About, the repo, the version as a row). Each card is a titled `section` with rows of label-left control-right, ≥44 px, hairline dividers, a footnote for the qualifying sentences; the status line sits beside the h1 so nothing jumps. About: the app's name and sentence from `VITE_APP_NAME`/`VITE_APP_DESCRIPTION` as the hero, then three iconed sections (filing as a five-step strip, where the data lives as a small table, licence and links) in two columns from 56 rem. Styles in `styles/you.css`; the old `.settings-*`/`.option*`/`.usage__*`/`.about*` rules left `shell.css` and `ask.css`. |
| H10 | Deployment IAM wildcards | **answer** | Each remaining `Resource: '*'` in `infrastructure/bootstrap.yaml` is justified inline beside it: Cognito, whose pool ids are service-generated, with every mutation conditioned on `aws:ResourceTag/Project` or `aws:CalledVia: cloudformation` and `UpdateUserPoolDomain` alone unconditioned because CloudTrail showed Cognito withholds the tag for it; KMS `CreateKey` (no ARN before creation; mutations tag-conditioned); event-source mappings (UUID-named, conditioned on `lambda:FunctionArn`); the read-only Describe/List/Validate calls and `logs:*LogDelivery`, which AWS does not scope; and one explicit Deny. A CloudFormation service role, `chintan-cfn-deploy` (`CfnDeployRole`), already exists. Nothing to change. |
| H11 | Instance metadata is the single source of the app's identity | **done** | `display_name` (required) and `description` (required) in `config/instances/*.yaml`, plus an optional `short_name` (≤ 12 characters, default `display_name`), reach the bundle as `VITE_APP_NAME` / `VITE_APP_SHORT_NAME` / `VITE_APP_DESCRIPTION` from `ci-build-site.sh`: the document title and meta description (`%VITE_…%` in `index.html`), the PWA manifest, the wordmark, the signed-out screen and About all derive from them at build time. The dead `pwa:` block (theme and background colours, a description that never shipped) is gone; colours are the design tokens' — the manifest keeps `--color-ground` as one constant in `manifest.config.ts`. `list-instances.sh` rejects a config missing either field, a long short name, or a field nothing reads; `check-vite-env.sh` counts `index.html` and `vite.config.ts` as readers. The deployed name stays "Chintan"; "Chintan Dev" was configuration nothing consumed. |
| H12 | Passkey card was headed "Sign in with a passkey" | **done** | The section is **Passkeys**; its lead says a passkey is set up on Cognito's sign-in page (the app cannot register one because the passkey belongs to the sign-in domain) and that the sign-in page then offers *Sign in with a passkey*. The button stays "Add a passkey on this device". |
| O1 | Delete-all left 16 August-era captures behind; Home showed three "Filed" bubbles under a count of 0 | **done** | Two defects. Purge listed a note's captures through GSI1, and a row written in August 2026 (only the `data` blob, nothing promoted) has no `gsi1pk`, so the sparse index never held it: `PurgeNoteArtifacts` now also reads the base table for rows without index keys (`Store.ListUnindexedCaptures`, a filtered Query — the honest fix over promoting on read, which would repair the index for a later purge but not this one). `chintanctl reconcile` judges `dangling_capture` from the blob when nothing is promoted (that is the finding for the owner's 16 rows; `--apply` deletes each with its objects), and gains `unlisted_note` (NOTE row without `note_id`) and `unindexed_capture` (CAPTURE row without GSI1 keys), both re-promoted from the blob under `--apply` through the store's own item builders. The two unlisted NOTE rows are reported with which shelf the blob says they belong on. |
| O2 | The API surfaced receipts for notes that were gone | **done** | `CaptureService.ListCaptures` drops an `appended` capture whose note has no row (`Store.NoteExists`, one key-only GetItem per distinct note per page, before the page is counted full); `needs_target`/`failed`/`no_content` are kept — they have no note by definition. The walk fallback lists through notes and could never reach one. |
| O3 | Old receipts are noise; the biggest text on Home was today's date | **done** | `isFilingRelevant` keeps an `appended` capture for 24 h (`FILED_RECEIPT_MS`, from `appended_at`, else `created_at`) and then leaves it to the note's Recordings tab. Home's h1 keeps its markup order (Notes, count, day — the accessible name still starts with "Notes") and the stylesheet swaps the roles: "Notes · 12" in the serif, the day above it in the eyebrow caps. |
| O4 | Per-user storage over time: `usage.storage` reads 0 after a delete-all though storage was paid for all month | **done** | A daily worker task `storage-snapshot` (`StorageSnapshotRule`, `rate(1 day)`) measures each tenant with `StorageService.Summarize` and ADDs `storage_byte_days += audio_bytes`, `storage_note_days += notes` onto `USAGE#<yyyy-mm>`, the day's reading on `USAGE#<yyyy-mm-dd>`. Idempotent per day by a condition on the month row's `storage_snapshot_day`. Tenants come from the GSI1 month rows of the last 13 months; a request or a snapshot writes one, so a snapshotted tenant stays in the set — the gap is a tenant with storage and no usage row in 13 months (`docs/design/usage-accounting.md`). `GET /v1/usage` exposes `storage.byte_days`, `storage.note_days`, `days[].storage_byte_days`; You shows "Stored this month: X GB·days (≈ $Y at S3 standard $0.023/GB-month)", priced on the client as an estimate. |
| O5 | You → Usage reads "27 recordings · 0.0 MB": `CaptureIndex.AudioBytes` is stamped only since 2026-09-05 | **done** | `chintanctl reconcile` finding `capture_size_unknown` (capture row with an audio key and `audio_bytes` 0). The bucket listing reconcile already walks carries every object's size, so no HeadObject: `--apply` writes that size into the row's record blob — where `audio_bytes` lives — as raw JSON with every other field kept, under the row's version. A capture whose audio is missing or empty is reported, not repaired. `--only <kind>` (repeatable) narrows what `--apply` touches, so one incident can be cleaned without accepting every repair. |
| F3 | An Ask question whose 202 landed after the library unmounted (a source chip tapped during the POST) was shown as "did not reach the server" on return, though it was answered and billed | **done** | `useAskThread` settles the turn from `mutateAsync`'s promise instead of per-call `mutate` callbacks, which TanStack drops once the component has unmounted; with no component left, the outcome is written straight into the saved thread (`updateStoredTurn`), which is what the panel comes back from. `useAskThread.test.tsx` unmounts between the POST and the 202. |
| F4 | The You screen labelled the language model "Openai" — the wire key for the OpenAI-compatible endpoint — while the tests asserted "MiniMax" from a hand-typed fixture key the backend never sends | **done** | `PROVIDER_LABELS` labels `openai` as "Language model (MiniMax)", the vendor from one commented constant (`LLM_VENDOR`); `usageRich` restates the generated fixture's provider keys rather than typing its own, and `usage.test.ts` asserts that every key the contract fixture carries has a label. Follow-up for the You-screen redesign: "Groq" could read "Speech to text (Groq)" for symmetry. |
| F5 | A pen that drifted under the swipe slop onto a neighbouring row and lifted there left the first row's gesture armed — and that row deaf to every later swipe — until remount | **done** | `useSwipeActions` clears an uncommitted gesture on `pointerleave` (a committed drag holds capture, so its up or cancel is its own and a leave is ignored) and treats `lostpointercapture` as a cancel. `SwipeRow.test.tsx` drives a `pointerType: 'pen'` drift. |
| F6 | `useSwipeActions` called `close()` during render, mutating the module-level open-row registry from a render | **done** | Render still derives the row's own closed state from the `enabled` prop (React's pattern); the registry is released from an effect once the disabled row has committed, alongside the existing unmount release. |
| F7 | "Save as note" wrote Markdown (`**Q: …**`, `- Roof repair`) into a plain-text body, shown verbatim in the Text tab and the Home snippet; a saved answer was then retrieved as a source for later answers (QA 2026-09-05 §3, §4) | **done** | `noteFromThread` writes `Q:` / `A:` paragraphs with a blank line between turns and a final `Sources:` paragraph, one title per line, and unwraps the answers' `**bold**`; the note is created with `tags: ['ask']` (`ASK_NOTE_TAG`) so retrieval can leave saved threads out — the backend is adding that exclusion. |
| F10 | The Home search placeholder was clipped on a 412 px phone ("Search titles, tags, tran") (QA §7) | **done** | `NotesScreen` reads `NARROW_FIELD_QUERY` (`max-width: 26rem`) through `useMediaQuery` and shows "Search notes" there — a placeholder cannot be swapped from CSS. Only the search-form block and one hook line changed; `setNarrowViewport` in the test setup drives it. |
| F9 | At 1280×800 the foot-anchored Details panel covered the conflict banner's "Use the newer version / Keep my edits" (QA §6) | **done** | `NoteDetailScreen` hands `NoteActions` `open={null}` while `editor.model.state === 'conflict'`; the panel's own state is kept, so it is back as it was once the conflict is resolved. One line in `NoteDetailScreen.tsx`, pinned in its suite. |
| F11 | The bulk selection bar wrapped into two uneven rows on a 412 px phone (QA §8) | **done** | `SelectionBar` is two lines by design — the count and Select all, then the actions and Cancel (`.selection-bar__toolbar` / `.selection-bar__line`) — so the break falls in the same place on every screen; targets stay 44 px. |
| F12 | No `theme-color` meta, so the browser chrome stayed pale in Nocturne (QA §9) | **done** | `index.html` carries one `theme-color` per colour scheme — each theme's ground as a literal, kept in step with `tokens.css` and `manifest.config.ts` by hand, as the manifest's is (the token lint reads `src/` only) — and `ThemeProvider` rewrites both tags from the computed `--color-ground` whenever the theme attribute changes, so an explicit choice wins over the media query. QA §10 checked alongside: `classifyMicrophoneError` maps `NotAllowedError` to the denied sentence; "This browser cannot record audio" in headless Chromium is the no-encoder path, reported before the microphone is asked for — now pinned by `audio.test.ts`. |
| F8 | Source chips were title-only, so five notes called "Gutter leak" made five identical chips (QA §4) | **done** | `sourceLabels` adds the note's date where titles collide within one answer — `AskSourceWire` carries only id and title, so the date is the device's cached copy of the library (`useCachedNotes`), the same `formatRowTime` the row shows — and numbers the ones it has no date for, or whose dates collide too. A tap still opens the note. |
| F13 | The You screen's "API requests" figure read as what the person did, though every authenticated request is counted and most are the app polling (`usage.api.requests` is the sum of the day rows since #52) | **done** | The fact row says so beside the number: "every call the app made, polling included". Copy only, in `UsageSection.tsx`. |
| F14 | A clean request bumps the note's `version` when it is accepted (#52), so an autosave already on the wire, or a draft being typed, lost the version check and was shown "this note changed somewhere else" though nobody touched the words | **done** | `useNoteEditor` carries the draft onto a newer server version whose text still reads as what it last saved (`sameText`: title, body, names, tags, language — not the cleaned-view settings, which a clean may set): on a refetch while dirty, and on a 409 by re-saving once with the server's version (`rebase` event). A 409 whose words differ is still the conflict prompt. `CleanedPanel` already refetches the note every 2 s after the 202, so the bumped version arrives without change. |
| R8b | Gateway-answered CORS preflights (#51) | **reverted** | Verified on staging by a browser check before prod: with `OptionsRoute` gone every preflight answered 401 and all browser requests failed (`net::ERR_FAILED`). The mechanism, now written into the template's `CorsConfiguration` comment (S4): HTTP API's `CorsConfiguration` auto-answers only a preflight that matches NO route, and `$default` matches OPTIONS and carries the JWT authorizer. The explicit OPTIONS route stays — the Lambda answers 204 and the gateway rewrites the CORS headers from `CorsConfiguration` on the way out, which is why the live 204 carries the gateway's seven header names. Every preflight is therefore one Lambda invocation, by decision. Prod gate rejected automatically; reverted in #54. |
| S4 | CORS comments contradicted each other and the mechanism that broke staging (`CorsConfiguration`, `OptionsRoute`, R8b, `middleware/cors.go`) | **done** | All four now say the one verified thing (see R8b): the gateway auto-answers only an unmatched OPTIONS; `$default` matches it and its authorizer answers 401; `OptionsRoute` sends it to the Lambda, whose 204 the gateway rewrites from `CorsConfiguration`. `cors.go`'s comment says the gateway's list wins and that its own list is for runs without the gateway. Not done here: the middleware's constant still lacks `X-Correlation-Id` and no test compares the two lists — a backend follow-up if a local run without the gateway ever matters. |
| S20 | README Configure table contradicted the schema on `display_name`, `description`, `short_name`, `daily_spend_cap_micros` | **done** | The rows now match `scripts/list-instances.sh` and the template: `display_name` and `description` required, `short_name` defaults to `display_name` and must fit 12 characters, the cap defaults to `5000000` ($5/day) with `0` the opt-out; `enable_alarms` says three or four alarms. H4 and H11 were marked done while this table said the opposite — a "done" that changes a README row includes the row. |
| S21 | Stale sentences left by #48/#52: `usage-accounting.md` and `AddStorageDay` still called the `update` helper's map aliasing "a defect being fixed separately"; the `usage` package doc counted `api_requests` on "month and day rows"; B14 said "no preconnect"; the README `reconcile` row listed one repair for five; the DLQ task lists omitted `storage-snapshot` | **done** | Each rewritten to what the code does at HEAD: `AddStorageDay` builds its own writes because it needs a condition and a SET beside the ADD; the request count lands on the day row and is summed on read; B14 names the three remaining items; `reconcile` names the five repairable kinds and `--only`; README (two places) and both instance configs list `storage-snapshot`. |
| S22 | `ci.yaml` promised "every tool fetched over the network names its version" while `cfn-lint` and `pyyaml` were unpinned and the golangci `install.sh` was fetched by a movable tag | **done** | `cfn-lint==1.56.0`, `pyyaml==6.0.3` (current on PyPI on 2026-09-05; verified against the template and the configs), and `install.sh` fetched at `27774aaf…`, the commit `v2.13.2` points to, with the version in a trailing comment and the bump recipe beside it. Supersedes H5's "fetched from its tag". Bumped by hand, as the header now says. |
| S23 | `deploy-backend.yaml` granted `id-token: write` to every job, `discover` and `test` included | **done** | Workflow level is `contents: read`; `build`, `deploy-staging` and `deploy-prod` — the three that assume an AWS role — carry `id-token: write` themselves, the way `deploy-frontend.yaml` already did. `release` keeps its `contents: write`. No other change. |
| S24 | `list-instances.sh` accepted `"`, `<`, `>`, `&` in `display_name`/`short_name`/`description`, which Vite writes into `index.html` unescaped | **done** | Refused, with a message naming the field, the character and why; `index.html` is unchanged (Vite's `%VITE_…%` substitution cannot escape per context, and the same strings reach the manifest and the bundle as JSON, so one representation that is right everywhere beats an escaped copy that must agree). `--self-test` proves every one of the four characters is refused in every field, and the CI job "Instance configs resolve" runs it, like its two sibling checks. |
| S26 | `storage-snapshot` under `rate(1 day)` could attribute a reading to the neighbouring day, or skip one, if the rule was created within seconds of midnight UTC (review 2026-09-05 r2, C-B8) | **done** | `StorageSnapshotRule` is `cron(0 3 * * ? *)`, far from the day boundary and from the aws-cost reading; the mechanism is written up in `usage-accounting.md` ("Which day a reading lands on"). Was not live: the prod rule fired around 20:00Z. |
| S35 | Docs nits: the D5 row carried two overlapping "Backend …" sentences; README said "four alarms" where the spend-cap alarm exists only with a non-zero cap; the `usage` package doc's `api_requests` sentence | **done** | D5 keeps the dated backend sentence only; README says "three or four" in both places; the package doc is fixed under S21. Left alone: `dynamo.go` says the August rows were "thirteen" and O1 says 16 — the code and the incident row were written by the same hand from different counts, and this pass could not tell which is right. |
| S1-fe | A save landing between the worker's body write and its row refresh carried the dictated paragraph away; the client half of the fix | **done** | `ApiError` carries the problem's `reason` and the response's `Retry-After`; `useNoteEditor` treats a 409 `append_in_progress` as a wait, not a conflict: the draft goes back to dirty (`saveDeferred`), the same PATCH is re-sent after `Retry-After` (2 s when unnamed), the F14 same-text rebase is deliberately not run (it would re-save straight into the window), and ten deferrals fall back to the ordinary conflict prompt. A 409 without `reason` behaves as before, so nothing changes until the backend's stamp lands. |
| S3 | An Ask 202 landing after the panel was *remounted* read as "did not reach the server", and Try again minted a new key — a lost response billed twice | **done** | Each turn keeps the body it was sent with (the server binds the key to those bytes); a young `asking` turn is kept on load and re-sent by the mounted hook under the same key, which the server answers with the original 202; a replay that meets the original still in flight (409) is retried a second later, a few times. Try again on a turn that never reached this side resends it the same way; a turn the server itself answered is still asked afresh. `useAskThread.test.tsx` covers remount-before-202, the in-flight replay and retry-after-a-lost-POST. |
| S10 | "Keep my edits" after a voice append discarded the recording's text without saying so | **done** | The conflict records what the newer version added after the last-saved body (`additionTo`, the shape of every append) and whether a recording was filed since the draft was loaded (a capture the note did not have as `appended`). The banner says "A recording was filed into this note while you were editing … keeping only your edits removes its text" and offers "Keep my edits and add the recording" (`keepBoth`: my words, theirs after them, on their version); when the addition cannot be separated, "Keep my edits" asks twice. Also fixed: the banner's buttons dispatched and called `saveNow()` in the same tick, so the resolution's save read `conflict` from the mirror and sent nothing — the resolutions now go through `commit`. |
| S14 | A failed Retry on the filing row was silent | **done** | The row renders the problem's own sentence (`userMessage`) under its actions as an alert, as `NoteRow` does for a swipe action; `.filing-row__error`. Tested with a refusing 409. |
| S16 | Ask "Save as note" used the per-call `onSuccess` F3 removed from the hook; an unmount before the 201 left the thread offering Save again — a second note | **done** | `mutateAsync().then(…)`: success ends the thread whether or not a panel is mounted (`thread.clear` writes storage directly when none is) and navigates only when one is — a jump from a screen the user has already left would be the wrong surprise; the error sentence is set only while mounted. Tested with the create held across an unmount. |
| S27 | "Try again" (usage card) and the passkey "OK" were text buttons under 44 px | **done** | `.text-link` is an inline-flex box with `--layout-touch-target-min` as its block and inline size; negative block margins measured against `lh` hand the extra height back to the line, so nothing around it shifts. The touch-target e2e runs in the deploy pipeline. |
| S28 | "Save as note" stripped only `**bold**`; `*em*`, `_em_`, `` `code` ``, `#` headings and list markers survived into the plain-text body | **done** | `markdown.ts` `toPlainText`: the renderer's own `parseBlocks`, emitted as text — inline marks unwrapped, bullets as bare lines, numbered lists renumbered (the number is meaning), rules dropped. Table-driven test, one row per construct. |
| S30/S36 | `--color-accent-soft` comment claimed >15:1 (14.08:1 measured); `BYTES_PER_GB = 10^9` against a price AWS quotes per 2^30 bytes (~7% high) | **done** | Comment corrected (14.08:1 on the ground, above 12:1 on the surfaces, Nocturne above 10:1 — AAA); `BYTES_PER_GB = 1024 ** 3`, with the reason beside it. |
| S1 | An editor save landing between the worker's body write and its index refresh silently deleted the paragraph just dictated: the version was read before the refresh bumped it and the ETag after the write, so both checks passed and the marker was carried without its text (review 2026-09-05 r2, D-H1) | **done** | Three parts. (1) `Pipeline.append` stamps the note row before it writes the body — one conditional `UpdateItem` (`Store.StampNoteAppend`: `version+1`, `appending_capture`, `appending_at`), never a whole-row `PutNote` — and `refreshNoteIndex` clears the stamp with its `PutNote`; an append that hands its claim back clears it with `Store.ClearNoteAppend`. Two appends to one note wait on each other's stamp (`appendStampWait`) so the row's one stamp always covers the write in flight. (2) `UpdateNote` refuses a body write with `ErrAppendInProgress` while the stamp is younger than `AppendClaimLease`, checked before the version so the client is not sent round the F14 rebase into the window; the body's ETag is read before the row so the version is the later witness. (3) The wire: `PATCH /v1/notes/{id}` → 409 in the version-conflict shape plus `reason: append_in_progress` and `Retry-After: 2`; the client repeats the same save unchanged. Tests: `pipeline/append_autosave_race_test.go` (the interleaving, the same-text re-save, the re-read on a moved version, the release), `service/notes_test.go` (read order, stamp before version), the contract fixture `problemAppendInProgress`. `docs/design/append-vs-autosave.md`. |
| S2 | Clean-note supersession was judged from the row's stamp, not the invocation's: with two requests in flight (structured then polished) and the newer run landing first, the older run read an unstamped row, called the model and stored structured over the polished view asked for last; a Lambda redelivery of a finished run billed a second call the same way (review 2026-09-05 r2, C-B1; reopens R5 narrowly) | **done** | `Invocation.RequestedAt` carries the stamp `RecordCleanRequest` wrote, through `Invoker.InvokeCleanNote(…, requestedAt)` on every hand-off (`RequestClean`, both auto-clean paths, the worker's self-invoke). `CleanNote` judges against it: the row must still carry exactly that stamp and mode, and an unstamped row means a newer run has already answered. An empty payload stamp (queued before the field) is judged from the row as before. Tests: `TestAnOlderCleanRunDeliveredAfterTheNewerOneWritesNothing`, `TestARedeliveredCleanRunThatAlreadyWroteDoesNotCallTheModelAgain`, the payload round trip, the hand-off carrying the stamp. |
| S9 | The clean-request stamp rewrote the whole note row (≤200 KB with `cleaned_body`, from whatever copy the caller held) and bumped `version`, which is what forced the F14 client-side rebase (review 2026-09-05 r2, C-B6) | **done** | `Store.StampCleanRequest` / `ClearCleanStamp`: one conditional `UpdateItem` on `cleaned_requested_at`/`_mode` under the row's version, version unchanged, no other attribute touched; `RecordCleanRequest` and `ClearCleanRequest` use them. Same pattern as S1's stamp. Tests: `TestStampCleanRequestTouchesOnlyTheStampAndLeavesTheVersionAlone` (both stores), `TestRecordCleanRequestMovesNeitherTheVersionNorTheCleanedView`. |
| S5 | Every internal `DrainPages` over notes re-read the whole partition once per page — the notes list is ordered in Go over a drain, so a cursor is a position, not a continuation — and Ask did it with `search_text` per row: ⌈N/200⌉ full drains, 640 MB at 2,000 notes (review 2026-09-05 r2, D-M1) | **done** | `Store.DrainNotes(tenant, DrainOptions{Shelf, MaxItems, IncludeSearchText, IncludeCleanedBody})` is one read of the partition, ordered and cut; Ask, routing candidates, the storage summary, note matching, tags, export and the capture walk use it (`NotesService.DrainNotes` logs the ceiling as the list does). The wire list keeps its pagination for the client corpus, but a page that asks for `search_text`/`cleaned_body` drains light and fetches the large field for its 200 rows with `BatchGetItem` (`hydrateNotes`, unprocessed keys re-asked). Measured with the fake counting Query calls: `TestDrainNotesReadsThePartitionOnceWhereDrainPagesReadsItPerPage` (40 → 10 queries for 37 notes at 4 per Query), `TestListNotesWithSearchTextHydratesOnlyThePage`, `TestAskAndRoutingReadTheNotesInOneDrainNotPerPage`. Server search still pages (`searchScanPages`); each page is now a light drain plus one hydration. |
| S6 | A 100-note purge batch repeated the full-partition `ListUnindexedCaptures` Query once per note and could not finish inside the API Lambda's 29 s; the failure was a 504 and then 409s on the idempotency key for a minute (review 2026-09-05 r2, C-B2 + D-M2) | **done** | `PurgeNotes` reads the unindexed captures once per batch and hands the set to every note's cascade (`purgeNoteArtifacts(…, legacy)`; the sweep's `PurgeNoteArtifacts` still reads for itself). The batch is bounded by time as well as count: `purgeTimeBudget` 20 s, or the request's own deadline less `purgeDeadlineMargin` 4 s when nearer; notes not reached are reported `failed` with `detail` "not attempted: … send the remaining notes again", a WARN and `NotePurgeBatchOutOfTime`. OpenAPI documents the outcome. Tests: `TestPurgeNotesListsTheUnindexedCapturesOncePerBatch`, `TestPurgeNotesStopsAtItsTimeBudgetAndReportsTheRestForRetry`, `TestPurgeNotesHonoursANearerRequestDeadline`. Client chunking (10–20 per batch) is the frontend stream's. |
| S11 | The UI offered Retry 10 minutes in (`STUCK_AFTER_MS`) while the append lease is 20 (`AppendClaimLease`); every tap in minutes 10–20 on an `appending` capture dead-lettered and e-mailed (review 2026-09-05 r2, D-L5) | **done** (backend) | The honest side is the API's: `RetryCapture` refuses an `appending` capture with 409 `ErrCaptureInFlight` ("the capture is still being processed; wait for it to finish or fail") while the claim lease is live, so a tap inside it is a message, not a dead letter. Documented on `POST /v1/captures/{id}/retry`. The client's threshold (20 min for `appending`, 15 for the other in-flight stages) is the frontend stream's; until then the early tap shows the 409's detail (S14). |
| S12 | Retry on a capture still `transcribing`/`cleaning` started a second concurrent delivery (review 2026-09-05 r2, D-L9) | **done** | `CaptureIndex.LastProgressAt` is stamped by every pipeline write (`persist`) and every hand-off (`RetryCapture`, `SetCaptureTarget`); `RetryCapture` refuses an in-flight capture with 409 `ErrCaptureInFlight` until the row has gone `service.CaptureStuckAfter` (15 min, the worker function's timeout) without a write — after which no worker can be alive on it — and then hands it over once; two taps are one delivery. Rows from before the field read `created_at`. `TestRetryCaptureRefusesAnInFlightCaptureUntilNoWorkerCanBeAlive`. |
| S13 | Three definitions of "terminal": `chintanctl reconcile` reported a `spend_capped` capture as `stuck_capture` (review 2026-09-05 r2, D-L3) | **done** | `model.IsTerminalStatus` (appended, no_content, failed, spend_capped, needs_target) is the one definition; `service.CaptureIsTerminal` delegates to it, `RetryCapture` and reconcile read it, and the frontend's `TERMINAL_CAPTURE_STATUSES` lists the same five. `TestReconcileDoesNotReportSpendCappedOrNeedsTargetAsStuck`. |
| S15 | `share_micros` could exceed `month_micros` two to three months back: the tenant month row never expires while the instance `SPEND#<day>` rows carry a 90-day TTL, so the denominator shrank under the numerator (review 2026-09-05 r2, D-L6) | **done** | `shareOf` clamps the fraction to one: a tenant total at or above the instance total is the whole month's AWS cost. `TestShareOfIsClampedToTheWholeMonth`. |
| S17 | Every captures list made a sequential `NoteExists` GetItem per distinct filed note, on the 1.5 s poll (review 2026-09-05 r2, C-B3) | **done** | `Store.NotesExist(ids)` — one `BatchGetItem` projected to the key — and the list asks it once per page for the notes not yet known (`noteExistence.learn`). `TestListCapturesAsksAboutAPagesNotesInOneRoundTrip`, `TestNotesExistIsOneBatchGetProjectedToTheKey`. |
| S18 | The "no user content in logs" CI check scanned only `backend/internal/provider` (review 2026-09-05 r2, C-B4) | **done** | `scripts/check-log-hygiene.sh` defaults to `provider`, `pipeline` and `service`. It flagged one line — the transcribed-capture log's `obs.Redact(result.Text)`, which was already shape-only; the shape is now taken on its own line so the emitter names no content. |
| S19 | `cleanRequestInFlight`'s 3-minute window is the model call's bound, not the run's; a run over 3 min lets a same-mode repeat start a second, duplicate-billed run (review 2026-09-05 r2, C-B5) | **done** (documented) | Kept at 3 minutes and documented on `CleanNoteTimeout`: with S2 the second run's re-stamp supersedes the first, so the view is never written twice and the cost of the rare slow run is one extra model call; the alternative — a window as long as the worker's 900 s lifetime — would leave a dead run's stamp answering "queued" for fifteen minutes with nothing running. |
| S25 | `Tokenize` kept the first 32 distinct terms in spoken order, so a long question whose subject came last ranked on the preamble (review 2026-09-05 r2, C-B7) | **done** | Over `MaxQueryTerms` the longest terms are kept (earliest spoken among equals), returned in spoken order. `TestTokenizeKeepsAtMostMaxQueryTermsAndPrefersTheLongest`. |
| S29 | A capture whose destination note was purged before the worker reached it still dead-lettered (round-1 B-L8, reopened) | **done** | `run()` on `ErrNotFound` for the destination clears `note_id` and `target_source`, sets `needs_target`, counts `CaptureDestinationPurged` and returns nil; the transcript and cleaned text are kept and the person's answer resumes from them. `TestACaptureWhoseDestinationWasPurgedAsksForANewNoteInsteadOfDeadLettering`. |
| S7 | One execution role for both Lambdas gave the internet-facing API the worker's grants: both provider keys via SSM, `dynamodb:Scan` across tenants, `sqs:SendMessage`, `budgets:ViewBudget` (review 2026-09-05 r2, D-M4) | **done** | `ApiFunctionRole` (`chintan-lambda-api-<i>-<e>`): item ops + Query + BatchGetItem on the table and its indexes, object CRUD and tagging under the bucket, `lambda:InvokeFunction` on the worker's live alias. `WorkerFunctionRole` (`chintan-lambda-worker-<i>-<e>`): the same plus Scan, SSM `GetParameter` on the two keys, self-invoke, SQS on the DLQ; `WorkerReadsBudgetPolicy` attaches to it alone; `WorkerLambdaEventInvokeConfig` depends on it. Both names keep the `chintan-lambda-` prefix the agent boundary and deny policies gate SSM/KMS on, and sit inside bootstrap.yaml's `chintan-*` CreateRole scope with the boundary attached. `dynamodb:BatchGetItem` is granted to both — S5 and S17 introduced the call. Deploy replaces the single role with two; the functions are updated in place. |
| S34 | `template.yaml` S3 notification suffix `/audio.bin` that no code path mints; worker `USER_POOL_ID`/`USER_POOL_CLIENT_ID` read by nothing in `cmd/worker` (review 2026-09-05 r2, D-L10) | **done** | Both removed. `bin` is only the download filename's fallback (`recordingFilename`); every accepted content type maps to webm/m4a/mp3/ogg/wav, each of which keeps its rule. |

## Capture screen 2026-09-21

| # | Item | Status | Notes |
|---|---|---|---|
| CS1 | Recording could only be stopped, then sent from review: two taps, and a screen to read, for the one thing the app is for | **done** | Send sits in the recording row. `store.stopAndSend(api)` sets a request the store's `dispatch` consumes when the machine leaves `stopping` — `review` sends, anything else drops it — because MediaRecorder hands over its last chunk after `stop()` returns, so `await stop(); await send()` would read a buffer still being written. The screen leaves to `captureReturnPath(noteId, true)` at once, as Send from review does; a failed upload lands on the filing row as before. Stop → review is unchanged. Tests: `store.test.ts` (same create body as Stop→Send under the real late-chunk timing; nothing sent when the stop finds no audio), `CaptureScreen.test.tsx`, `e2e/capture.spec.ts` (a real recorder: the create carries a length and a size). |
| CS2 | The controls were text pills; the owner asked for graphic controls that read at a glance while walking | **done** | Round icon discs with a word under each (the word is the accessible name, so every existing `getByRole('button', { name })` still holds): Cancel ×, Pause ‖ / Resume ▶, Stop ■, Send ↑ while recording; Discard, Re-record ↺, Send at review; Discard/Close and Try again on failure; Done after an upload. Send is the row's only filled shape (ink, like the app's other primaries; the accent stays the record button's). 64 px discs under a coarse pointer, 56 px under a fine one (`--capture-control-size`); the row is four equal `minmax(0, 1fr)` grid columns, so it cannot wrap at 320 px — pinned by a Pixel-7-at-320 e2e. `Icon.tsx` gains `pause` and `send` in the 1.75 px set; Re-record wears the existing `restore` arrow. |
| CS3 | The capture screen's styles sat in three places in `shell.css` | **done** | Moved unchanged into `styles/capture.css` (imported after `shell.css`) in its own commit, then restyled there. The live canvas (`.waveform`), the review player and the target chooser stay in `shell.css`; the shared filled-primary rule there now names `.capture__control--primary .capture__control-disc`. |
| CS4 | The PWA shortcut (`/capture` starts recording at once) | **open** | Unchanged on purpose; the owner's remark is queued as a question. |

## Checklists 2026-09-21

| # | Item | Status | Notes |
|---|---|---|---|
| C1 | A note can be a checklist: `kind` on the model, the store and the wire; `GET /v1/notes?kind=checklist` | **done** (backend) | `NoteIndex.Kind` (`""` \| `checklist`), promoted like `language` so legacy rows are plain with no backfill and `chintanctl reconcile` re-promotes it via `NoteItemAttributes`. Wire `kind: note \| checklist` required on `Note`/`NoteDetail`/search-corpus rows; accepted on `POST`/`PATCH` (400 `kind must be note or checklist`); the server never converts the body. `?kind=` filters after the page as `?tag=` does. OpenAPI; fixtures `noteDetailChecklist`, `notesPageChecklists`. |
| C2 | A recording filed into a checklist becomes one item | **done** | `Pipeline.append` writes `- [ ] ` + the cleaned text collapsed to one line (≤2,000 runes; a long recording is still one item — the split is the cleaned view's). The marker keeps its line before the item, so delete and move cut exactly that item (`capture_edit_checklist_test.go`, `checklist_append_test.go`). |
| C3 | Clean mode `tasks`, the only mode for a checklist | **done** | `model.NoteCleanTasks`; `EffectiveCleanMode` answers tasks for every checklist; `service.CheckCleanMode` is the one rule for `PATCH cleaned_mode` and `POST …/clean` — 400 `cleaned_mode must be tasks for a checklist` / `cleaned_mode must be polished or structured`. Prompt in `internal/cleanup`; `NoteOutput` holds the answer to `^- \[( \|x)\] \S` per line, blank lines dropped, ≤500 items, else the verdict "the cleanup model returned nothing usable" with the previous view kept. Fake provider answers tasks with a valid list. Fixture `noteDetailChecklistCleaned`. |
| C4 | Design note | **done** | `docs/design/checklists.md`: data model, append rule, tasks mode, why the body stays the single source of truth. |
| C5 | Frontend compile fixes riding the backend PR | **done** (minimal; frontend stream's versions win) | The generated fixtures are typechecked against `schema.ts`, and every checklist Note carries `kind` and `cleaned_mode: tasks`, so four small edits keep the backend PR green: `NoteWire.kind?` + `NoteKind`, `'tasks'` in `NoteCleanMode`, the two mode maps in `cleaned.ts`, `NoteDraft.cleaned_mode` typed by the alias, and the mode picker filtering `tasks` out for plain notes. |
| C6 | "Subsequently added notes should also be created as checklists" — new notes default to checklists? | **queued** (owner) | Read as "recordings added to that note become items", which C2 implements. If it means a default for new notes, that is a settings switch; awaiting the owner. |
| C-fe1 | A note can be a checklist: the Text tab becomes **Items** — open items as rows with a 44 px checkbox and a one-line field, an "Add an item" row, a **Done (N)** section below with reopen and delete (owner's request, 2026-09-21) | **done** (frontend) | `features/notes/checklist.ts` parses and writes the GitHub task-list body (blank lines dropped, a prose line shown as an open item and normalised on the next write, `[X]` read as done, CRLF tolerated); `ChecklistEditor.tsx` renders it and writes through `editor.edit({ body })` and the existing autosave, so a tick rides the same version check and conflict prompt as typing. Enter starts the next item, Backspace in an emptied one removes it, a tick or delete saves at once, "Marked done"/"Reopened" is announced. Tests: `checklist.test.ts`, `ChecklistEditor.test.tsx`, `ChecklistNote.test.tsx`, `e2e/checklist.spec.ts`. |
| C-fe2 | Home: a **Checklists** chip after All, `?kind=checklist`, filtering the server list and the device's copy; `NoteRow` shows the glyph, the open items as the snippet and "3 of 7 done" (a floor, "7+", when the 500-rune snippet was cut) | **done** (frontend) | `NotesScreen.tsx`, `NoteRow.tsx`, `Icon.tsx` (`checklist` glyph). The chip carries its count on the same terms as Archived — one cheap list query. `NotesScreen.checklist.test.tsx`, `NoteRow.checklist.test.tsx`. |
| C-fe3 | Details: a **Checklist** switch converting the body both ways and sending `kind` with it; the Cleaned tab is **Split up** for a checklist — no mode picker, the `tasks` result previewed as read-only rows, **Use this list** and **Regenerate**; the meta line says "3 of 7 done" instead of the word count | **done** (frontend) | `NoteActions.tsx` (`ChecklistSwitch`), `CleanedPanel.tsx`, `NoteDetailScreen.tsx`. `kind` rides the draft like `language`, sent only when it changed so a save to a backend without the field is untouched; `schema.ts` keeps `kind` optional until the backend that sends it lands, with `noteDetailChecklist`/`noteRowChecklist` in `__fixtures__/pending.ts` to build against. The Split up request names no mode: the server applies `tasks` itself. |
| C-fe4 | Find in an Items tab shows the raw `- [ ]` lines in the mirror | **next** | The find bar's mirror is the body's text, marked; for a checklist that is the task-list syntax. Fine for finding a word; a row-shaped mirror is a small follow-up once the tab has been used. |

## Round 3 — 2026-09-21

| # | Item | Status | Notes |
|---|---|---|---|
| T1/T31 | Swipe-to-reveal was dead on touch: a phone's implicit pointer capture sits on the pressed `.note-row` button, and the row's own `setPointerCapture` at the slop moved it, firing a bubbling `lostpointercapture` from the button that `useSwipeActions` read as the browser cancelling the drag. The gesture was nulled one frame after it committed, `--swipe-x` stayed `0px` on Home, the archive and recording heads, and the lift's click opened the note instead (since e1e11d5, #53; bisected b055156 → 924f0a0). No e2e project had touch enabled, so jsdom — which has no pointer capture — was the only place the gesture ran | **done** | `onLostPointerCapture` returns early unless `event.target === event.currentTarget`: a descendant handing its capture to the row is not the row losing it; the pen-drift case the handler was added for is still `onPointerLeave`'s. `SwipeRow.test.tsx` stubs the row's `setPointerCapture` to fire the button's `lostpointercapture` mid-drag and asserts the drag follows and settles open (fails at 6e090ff, passes with the guard). New `e2e/swipe.spec.ts` on Chromium as a Pixel 7 with touch, driving `Input.dispatchTouchEvent` through CDP against the stub API: a sideways drag opens exactly one tray with `--swipe-x` equal to minus the tray width and does not open the note; a drag down scrolls and opens nothing; a second row's swipe closes the first. |
| T2 | Recordings made from Home were transcribed in the tenant default; the destination note's language never applied | **done** (backend) | After routing (or a person's `/target`) lands on a note whose `language` is a code other than the one sent, the worker clears `RawKey`/`SegmentsKey`/`RoutedKey` and transcribes once more with the note set, strips instructions with the destination pinned, then cleans. `CaptureIndex.Language` (what was asked for) and `LanguageDetected` are written with `RawKey`, so a retry does not make a third call. A router-created note starts in the language its recording was transcribed in unless that was `auto`. `language_test.go` covers both cases. Per-recording language pill: owner (§4 item 2). |
| T3 | Auto-detect re-scripts Malayalam as Tamil and drops the other language from mixed speech; the copy recommended it | **done** (copy) | `types.go` comment, OpenAPI `default_language`, and row D7 now say auto picks one language per recording and drops or re-scripts the rest; mixed speech does best with the main language's code. The You-screen and Details hints are S3/S4; setting the default to Malayalam is the owner's (§4 item 1). |
| T7 | No way to transcribe a recording again in another language | **done** (backend + OpenAPI) | `POST /v1/captures/{id}/retranscribe {language?}` → 202 `Capture` back in `transcribing`: refuses an in-flight capture (RetryCapture's rule), one whose audio the retention rule deleted and one whose note is archived (409), clears the transcript keys (the objects stay downloadable until the worker writes over them) and the append claim, stores `RequestedLanguage` (outranks the note's and the default, and stays so T2 never undoes it), invokes the worker. The worker replaces the paragraph in place by its marker; a checklist item as one line with its tick kept. A retry of the append inside its lease compares the paragraph under the marker with this attempt's text (`paragraphInNote`), since the marker alone now sits over the earlier paragraph too. Fixture `captureRetranscribing`. The ⋮ menu item is S3. |
| T9 | Per-capture cleanup prompts and the router's title rules carried no language rule | **done** | Faithful, polished and the router: keep the transcript's language and script exactly, never translate or transliterate, a phrase you cannot make sense of stays as spoken, a title is in the speaker's language. Where the language is known (the code sent, else the code for the name Whisper detected) the cleanup user prompt opens with "The transcript is in Malayalam (ml)."; `LLM.Cleanup` gains a `language` argument. `prompt_test.go` in both packages asserts the rule. |
| T10 | The worker log recorded neither the language sent nor the one detected | **done** | `transcribed capture` carries `language_sent` (`auto` or the code) and `language_detected` (Whisper's name); `TranscribedLanguage{Outcome: match, mismatch, auto, undetected}` makes the mismatch rate graphable; a detected name outside the code table counts as a mismatch. The code↔name table (`model.LanguageName`/`LanguageCode`) mirrors the frontend's 28 languages. |
| T11 | The note `verbatim` switch was stored, promised and never read | **done** (pipeline) | `clean()` takes the note's switch: for a verbatim note `CleanKey` points at the routed or raw key and the capture is marked cleaned, so the append reads the words as spoken and no model is called; `CaptureCleanupBypassed` counts it. The verifier's repro is the regression test. The Details checkbox is S3. |
| T32 | `SetCaptureTarget` did not refuse an in-flight capture | **done** | Mirrors `RetryCapture`: refused with 409 unless terminal or `CaptureStuck`, so a capture still transcribing is not re-invoked beside the live delivery. One table test. |
| T40 | Fallback note titles carried a UTC clock beside the row's local one | **done** | The first six words of the transcript (≤40 characters, trailing punctuation dropped); only an empty transcript gets "Voice note <YYYY-MM-DD>" with no clock. No wire change. |
| T58 | Malayalam chillu spelling variants never matched each other in Ask or search | **done** (backend) | `ask.FoldScript` maps the five `<consonant, virama, ZWJ>` sequences onto the atomic chillus and drops stray U+200C/U+200D; applied in `SearchText` (stored), `searchTerms` (query), `Tokenize` (question) and on the compare side to the title, aliases, tags and snippet in `matchNote` and `ask.score`. Rows written before this keep their old `search_text` until the next body write: **run `chintanctl backfill-search-text --instance <name> --apply` after deploy**. `foldText`/`localSearch` are S6. |
| T12 | A Clean tap's stamp was erased by any concurrent whole-row `PutNote`: `StampCleanRequest` leaves the version alone, so a save from a copy read before the tap landed under an unchanged version and wrote `cleaned_requested_at` back from that copy; the worker then judged itself superseded and the Cleaned tab polled for a view that never arrived (BACKEND-2) | **done** | `PutNote`'s condition is now `(version) AND (attribute_not_exists(cleaned_requested_at) OR cleaned_requested_at = :stamp)` with the stamp the caller's copy carries, failing as `ErrVersionConflict`, which every caller re-reads on and the F14 rebase absorbs; `memory.Store` mirrors it. The worker's finish write used to clear the stamp inside the same `PutNote`, which the store cannot tell from a stale copy, so `writeCleanNoteRow` now carries the stamp as read and clears it with `ClearCleanStamp` (conditional on that stamp; a failed clear is logged, the view is stored). `TestPutNoteFromACopyReadBeforeACleanStampIsRefused` (both stores), `TestPutNoteAdmitsARowWrittenBeforeTheStampWasPromoted`. |
| T13 | The idempotency middleware recorded every status under 500 for a day, so a 429 spend_capped was replayed for the "Resend tomorrow" the UI promises, as were 409 capture-in-flight and 409 append_in_progress; a replay also forced `Content-Type: application/json` onto a problem body (BACKEND-3) | **done** | `settled(status)`: 409, 429 and 5xx are not recorded and the deferred abandon releases the claim. `IdemRecord.ContentType` is stored (`idem_content_type`) and written back on replay. Retry-After is not stored: after this change no recorded status carries one. `TestATransient429IsNotPinnedToTheIdempotencyKey`, `TestATransient409IsNotPinnedToTheIdempotencyKey`, `TestAReplayedProblemKeepsItsContentType`. OpenAPI wording ("only settled responses are replayed") is for the stream that owns `openapi.yaml`. |
| T26 | Every fresh API container paid ~270 ms on its first request because the JWKS fetch and the DynamoDB connection opened inside the handler; Home's five GETs spawn five to seven containers on an idle launch (PERF-1) | **done** | `CognitoVerifier.Warm(ctx)` refreshes the key set; `cmd/api` `init()` runs it concurrently with one `GetSettings` on the sentinel partition `__warmup__` (one GetItem; the role has item operations, not DescribeTable), bounded by 5 s, waited on before the first invocation; a failure is a warning. `TestWarmFetchesTheKeySetSoTheFirstVerifyDoesNot`. Verify after deploy with the perf lens's `firstreq.py`. |
| T55 | Export snapshots (`export.json`, `job.json`, the whole corpus) were written to S3 untagged and never expired (SECURITY-6) | **done** (backend half) | `Objects.PutTagged` binds tags on the write (S3 `Tagging`, the memory fake replaces the tag set); both export objects carry `chintan-artifact=export`. The one-day lifecycle rule on that tag is the infra stream's half. `TestExportObjectsCarryTheExpiryTag`. Follow-up: `chintanctl reconcile` classifies `tenants/<id>/exports/…` as `unknown_object`; it should skip the group (or the erase test should seed one) — chintanctl is outside this stream. |
| T12 | A Clean tap's stamp was erased by any concurrent whole-row `PutNote`: `StampCleanRequest` leaves the version alone, so a save from a copy read before the tap landed under an unchanged version and wrote `cleaned_requested_at` back from that copy; the worker then judged itself superseded and the Cleaned tab polled for a view that never arrived (BACKEND-2) | **done** | `PutNote`'s condition is now `(version) AND (attribute_not_exists(cleaned_requested_at) OR cleaned_requested_at = :stamp)` with the stamp the caller's copy carries, failing as `ErrVersionConflict`; `memory.Store` mirrors it. The pin alone turned the item's own scenario (CleanedPanel's `saveNow()` then `regenerate()` in one tick) into a false conflict prompt: the stamp does not move the version, so the client's F14 rebase (which needs `theirs.version > current.version`) cannot absorb a same-version 409. `UpdateNote`, `ArchiveNote` and `RestoreNote` therefore write through `putCarryingStamp`: on a conflict it re-reads once, and when only the stamp moved it copies the stamp onto its row and writes again; a moved version is a real conflict, answered with the stored version rather than 0. The worker's finish write used to clear the stamp inside the same `PutNote`, which the store cannot tell from a stale copy, so `writeCleanNoteRow` now carries the stamp as read and clears it with `ClearCleanStamp` (conditional on that stamp; a failed clear is logged, the view is stored). `TestPutNoteFromACopyReadBeforeACleanStampIsRefused` (both stores), `TestPutNoteAdmitsARowWrittenBeforeTheStampWasPromoted`, `TestAWholeRowWriteThatCrossesACleanTapCarriesTheStamp`, `TestAWholeRowWriteThatLosesToAnotherWriteReportsTheStoredVersion`. |
| T13 | The idempotency middleware recorded every status under 500 for a day, so a 429 spend_capped was replayed for the "Resend tomorrow" the UI promises, as were 409 capture-in-flight and 409 append_in_progress; a replay also forced `Content-Type: application/json` onto a problem body (BACKEND-3) | **done** | `settled(status)`: 409, 429 and 5xx are not recorded and the deferred abandon releases the claim. `IdemRecord.ContentType` is stored (`idem_content_type`) and written back on replay. Retry-After is not stored: after this change no recorded status carries one. `TestATransient429IsNotPinnedToTheIdempotencyKey`, `TestATransient409IsNotPinnedToTheIdempotencyKey`, `TestAReplayedProblemKeepsItsContentType`. `Idempotency-Key`'s OpenAPI description says only a settled response is replayed and that the replay carries the recorded Content-Type. |
| T26 | Every fresh API container paid ~270 ms on its first request because the JWKS fetch and the DynamoDB connection opened inside the handler; Home's five GETs spawn five to seven containers on an idle launch (PERF-1) | **done** | `CognitoVerifier.Warm(ctx)` refreshes the key set; `cmd/api` `init()` runs it concurrently with the readiness probe (`ReadinessService.Check`: one GetItem on `__readiness__` and one S3 GetObject, so the S3 client is warm too; the role has item operations, not DescribeTable), bounded by 5 s, waited on before the first invocation; a failure is a warning. `TestWarmFetchesTheKeySetSoTheFirstVerifyDoesNot`. Verify after deploy with the perf lens's `firstreq.py`. |
| T55 | Export snapshots (`export.json`, `job.json`, the whole corpus) were written to S3 untagged and never expired (SECURITY-6) | **done** (backend half) | `Objects.PutTagged` binds tags on the write (S3 `Tagging`, the memory fake replaces the tag set); both export objects carry `chintan-artifact=export`. The one-day lifecycle rule on that tag is the infra stream's half. `TestExportObjectsCarryTheExpiryTag`. `TestEraseRemovesOneTenantAndProvesIt` seeds an export snapshot and proves `erase` removes it (T55-r). |
| T68 | The gateway's 401 names the JWT failure the backend hides (SECURITY-7) | **done** (code half) | `auth.ErrUnauthenticated` and `middleware.Auth` comments say the uniform 401 is a property of the Lambda path; the API Gateway JWT authorizer's own 401 carries a `WWW-Authenticate` naming the failure. README sentence is the docs stream's. |
| T55-r | `chintanctl reconcile` classifies `tenants/<id>/exports/…` as `unknown_object` once an export exists (the object walk's `switch ref.Group` has no `exports` arm) | **next** | One `case "exports": return nil` in `reconcile.go`'s object walk plus a seeded export in its test; owned by whichever stream next touches chintanctl. |
| T8 | The capture screen never said which language Whisper would be told | **done** (capture-screen half; the recording row's "Heard as" chip is S3's) | `TargetChooser` shows "Transcribed as മലയാളം" under the pill — the target note's `language`, else `default_language`, else English, in the language's own script; "Language detected per recording" for `auto`. Settings are fetched with the list, i.e. not before the microphone is live. No wire change. |
| T14 | Retry and the reconnect resend could never escape an expired presign | **done** | `store.send` passes `serverCaptureId`, so the uploader's re-key branch runs. Store test: create lands, PUT fails, 45 min pass, Retry → second create under a resume key, PUT to the fresh URL. |
| T15 | `store.send` had no state guard; a send outside review pruned a buffer still being written | **done** | `send` returns unless `review` or `failed`; #62's `stopAndSend` is the live path. Test calls `send` during `stopping`. |
| T16 | A capture failure left a dead screen with a live target chooser and no way back | **done** | `state === 'failed'` renders a card (icon, sentence at body size, "Allow the microphone in your browser's site settings, then try again" for permission-denied, primary Try again that re-runs `start(noteId)` for permission-denied and no-microphone, Close/Discard); chooser disabled while failed; `alert` icon added. |
| T19 | "Filed" receipts did not say into which note | **done** (FilingRow half; `Recordings.tsx`'s `filedLabel` is S3's) | `Filed into “<title>”` from the device's cached library (`useCachedNotes`), falling back to "Filed"; the whole receipt is the Open control with a chevron and a hidden "Open the note" in its name (the chevron is the button, named by the title via `aria-labelledby`, its `::after` stretched over the card, so the title's `role="status"` node is the one the moving row rendered and the flip to "Filed" is announced); Dismiss is an icon button. `.filing-row__open-icon` in `shell.css` is now unused (left for the Home stream's file). |
| T34 | A failed upload aimed at a note was invisible from Home until a reload | **done** | `useLocalUpload` shows a `failed` local upload on Home regardless of target; a moving one still belongs to the note's Recordings tab. |
| T35 | The filing row offered Retry at ten minutes; the server refuses until fifteen or twenty | **done** (client; exact once the API carries `last_progress_at`) | `retryAccepted`: 20 min for `appending`, 15 otherwise, measured from `created_at` today — exact for a capture that never moved, early by however long it did move for one that stalled after transcribing began, and that tap shows the 409's sentence. `CaptureWire.last_progress_at` is declared and read when carried; the server's rule is on that field (`CaptureStuck`), so the residual closes when S5 adds `LastProgressAt` to `handler.Capture`/`captureOf` and the `Capture` schema in openapi.yaml and regenerates the fixtures. The "still not done" copy stays at ten minutes; only the button waits. |
| T36 | `PasskeyCard.test.tsx` flaked in the full run | **done** | The redirect test waits for pathname and search together, and `findByText(/passkey added/i)` gets a 5 s window. |
| T38 | `UpdatePrompt` ignored a worker already installing when it mounted | **done** | After `ready`, `registration.installing` is watched too; three tests in `UpdatePrompt.test.tsx` (waiting, already installing, found later). |
| T39 | Recording screen: live state not signalled | **done** | Pulsing accent dot before "Recording" and the clock in `--color-accent-strong` while `isLive`; reduced motion stops the pulse. |
| T42 | The target chooser listed identical titles with nothing to tell them apart | **done** (TargetChooser half; MoveSheet is S3's) | Each option carries `formatRowTime(updated_at) · <first 40 graphemes of snippet>…` as a meta line, part of the accessible name; cut with `Intl.Segmenter` so a Malayalam vowel sign is never stranded, and the cut is marked. |
| T56 | Hardcoded "Chintan" in the microphone sentence and the update prompt | **done** (`machine.ts` and `UpdatePrompt` halves; SignedOut/NotFound/nudge are S4's) | Both read `config.appName`. |
| T6 | The note screen on a phone was 55 percent chrome, with the note bar's "Record into this" 30 px above a tab-bar mic that recorded into a *new* note | **done** (owner reviews on the phone, §4 item 15) | The tab bar's mic is contextual while `/notes/:id` is open — `captureInto(id)`, named "Record into this note", an "Into this note" pill on the bar's top edge so the bar keeps its height. The action bar is gone; Details · Share · Archive (Restore · Delete forever) are an `OverflowMenu` beside Find in the header ("Note actions"). The disclosures open as a drawer above the tab bar and close from their heading. The meta is one line ("5 Sept · house · 3 rec · 0:17 · Malayalam"); the word count is gone. New rules in `styles/notes.css`; shell.css's `.note-bar*`, `.transcript__copy` and `.player__actions` rules are now unreferenced and can go once the streams have merged. E2E specs reach the actions through `noteAction()` in `e2e/fixtures.ts`. |
| T43 | The Details/Share disclosure had no edge and read as the body cut off mid-line | **done** | `.note-panel` is a raised sheet with a hairline, `--shadow-sheet` and rounded top corners (`notes.css`). |
| T18 | Deleting one note asked for its title to be typed while bulk and recording deletes asked for "delete" | **done** | `NoteRow` and `NoteMenu` ask for "delete"; the title stays in the dialog's sentence. |
| T19 | Every appended recording row said "Filed" | **done** (Recordings half) | `filedLabel` is empty for `appended`; moving and failed states keep their words. The library receipt's `Filed into "<title>"` is S2's. |
| T41 | An open recording stacked five control clusters and a tutorial sentence | **done** | "Copy this transcript" / "Copy this cleaned text" are items of the row's More menu, offered while the row is open with its text in hand (Safari refuses a clipboard write outside the gesture); the player's own Download button is gone, the menu's fetches with `no-store`; the switch sits right of the eyebrow; "Tap any line to jump there" shows until the first tap on the device (`chintan.transcript-hint-seen`). `copyText` exported from `CopyButton.tsx`. |
| T7 | No way to transcribe a recording again in another language | **done** (frontend, against the S5 contract) | `retranscribeCapture` → `POST /v1/captures/{id}/retranscribe` (no body: the server applies the note's effective language). A settled row's More menu offers "Transcribe again in Malayalam" ("(auto-detect)" under auto); the 202's capture goes onto the cached note so the row wears the stage strip again; a 404 is said as "not available on this server yet". Stubbed in `e2e/fixtures.ts`. Not yet in `contract-requests.test.ts` — the Go contract test replays that fixture; add `retranscribeCapture` there once S5 is on main. |
| T8 | Whisper's detected language was stored and never shown | **done** (row half) | `parseSegmentsDocument` keeps `language`; `loadCaptureArtifacts.detectedLanguage`. The open row shows "Heard as Tamil" when the name differs from the effective language (always under auto); under a chosen language the chip is a button that runs T7. The capture screen's "Transcribed as" line is S2's. |
| T11 | The `verbatim` switch was stored, promised and unreachable | **done** (frontend) | Details gains "Word for word — Keep recordings as spoken"; `verbatim` rides the draft like `auto_clean`, sent only when flipped. The pipeline half (append the raw transcript, call no model) is S5's. |
| T42 | The Move sheet listed identical titles with nothing to tell them apart | **done** (Move half) | Each option's meta line is `formatRowTime(updated_at)` and the first 40 characters of the snippet (a checklist's open items). The target chooser is S2's. |
| T60 | Only `<html lang="en">`: Malayalam text announced and spell-checked as English | **done** | `lang` from `contentLanguage` (note's language, else default, none under auto) on the body textarea, the find mirror, the cleaned view and each transcript. |
| T3 | The note Details hint recommended nothing about mixed speech, and said a routed recording keeps the default | **done** (Details hint) | "For every recording that lands in this note — made into it, or filed here automatically." plus "Mix Malayalam and English in one recording? Choose Malayalam. Auto-detect picks one language per recording and drops or re-scripts the other." The You-screen hint, `types.go` comment, OpenAPI text and row D7 are S4's and S5's. |
| T3 | Auto-detect re-scripts Malayalam as Tamil and drops Malayalam from code-switched speech; the app's copy recommended it (LANG-1) | **done** (You hint; the Details hint is S3's, the comment/OpenAPI text S5's, the setting the owner's) | One line under the Transcription language row on You: "Mix Malayalam and English in one recording? Choose Malayalam. Auto-detect picks one language per recording and drops or re-scripts the other." `SettingsScreen.tsx`, test in `SettingsScreen.test.tsx`. Row D7's "auto is the closest to mixed" is S5's to correct. |
| T5 | "Keep recordings for" showed a retention the server does not store, and audio was deleted before the day it named (PRODUCT-3, UX-11) | **done** | A native `<select>` of the five tiers the server keeps (Keep forever · 7 · 30 · 90 · 365 days), so what is shown is what is stored; the draft is derived from the cache plus the in-flight PUT rather than copied once, so a coerced value re-renders from the PUT response; the footnote and About say the tier applies to recordings made from now on and earlier recordings keep the retention they were uploaded with. The debounce that a typed number needed is gone with it. |
| T17 | Home spent the top 45 percent of the phone on labels and chrome (UX-5) | **done** (owner reviews) | On the library the shell banner holds only the offline pill and the wordmark sits small at the heading row's right with the day beneath (`NotesScreen.tsx`, `AppShell.tsx`); the Search \| Ask segment is one ✦ toggle inside the field's trailing edge, so the field spans the row; the Archived chip is hidden while its count is 0 and an "Archive" row link sits at the list's end; the passkey nudge is one 44 px row "Sign in faster next time — Set up · Not now" (`PasskeyNudge.tsx`). New rules in `styles/home.css`. `AskModeToggle.tsx` removed; its `.field-mode` rules in `ask.css` are orphaned (S2/S3 own that sheet's neighbours; delete next round). |
| T20 | You was three screens long; the Usage card was a third of it and buried Passkeys and About (UX-9) | **done** (owner reviews) | Usage is one `RowLink` in About & support ("Usage this month  $0.024 ›") that opens `/usage`, a lazy route rendering the unchanged `UsageSection` (`screens/UsageScreen.tsx`, `ROUTES.usage`); cards ordered Recording · Appearance · Passkeys · Your data · About; each card's foot keeps one sentence with the rest behind a native `<details>` "More" (`SettingsCard` `more`); "Before the split began" → "Earlier this month"; the "Tallest bar" line dropped (the peak stays in the figure's description and hover). `PasskeyCard`'s lead is untouched (not this stream's file). |
| T21 | Ask showed two input fields for one thread, an unbounded chip wall, and could post twice (UX-10) | **done** | The panel's follow-up form is gone; the library's field carries "Ask a follow-up…" and submits while a thread is open; `useAskThread.ask` drops a question while the last is unanswered; an answer shows three source chips and a "+n more" chip that expands; same-titled sources are told apart by the note's date, then the first words where their snippets differ (`distinguishingWords`), and numbered only when the device knows neither. `.ask__follow-up` in `ask.css` is orphaned. |
| T28 | About's privacy statement was written for the owner-as-user and wrong on the model count (PRODUCT-4) | **done** (owner sign-off on the wording) | "Your data" uses the section 4 item 9 wording: whose AWS account, what the operator can read, every use of the language model (route, clean, whole-note rewrite, Ask), neither provider asked to retain; "no accounts of yours with anyone else" → "the app talks only to the instance's AWS account". |
| T30 | Export and erase existed for the API and the operator but not for the user (PRODUCT-6) | **done** (export half) | A "Your data" card on You with "Download my notes (JSON)": `startExport`/`getExport` wrappers back in `endpoints.ts` (and recorded for the Go replay), `ExportCard.tsx` posts, polls to `ready`, fetches the presigned file and saves it through the app's one `saveBlob` path via `DownloadButton`. "Delete my account" is the owner's decision (section 4 item 11). |
| T46 | A Home mount fanned out five GETs and five preflights; tags existed only for chip names (PERF-3) | **done** (frontend half) | Tag chips are the tags on the active notes the device holds (the corpus's light projection) unioned with the server's unfiltered page, sorted; `GET /v1/tags` is no longer asked by Home (the tag editor still uses it); pull-to-refresh no longer invalidates it. The archived query stays for its count. |
| T47 | One 548 KB chunk with no route splitting (PERF-6) | **done** (all but `zipRecordings`) | `React.lazy` for Settings, Usage, About and Capture in `router.tsx` behind one `<Suspense>` in the shell, and for the Ask panel in `NotesScreen.tsx`; every chunk is already in the precache glob. The dynamic import of `zipRecordings` (fflate) lives in `features/notes/Recordings.tsx`, S3's file — **follow-up for S3**. |
| T48 | The note screen fetched `/v1/settings` after the note, a second waterfall step for a language label (PERF-7) | **done** | `AppShell` prefetches the settings once after the auth gate with `staleTime: Infinity`; `useSaveSettings` already writes the cache. Not on the capture screen, so the Record shortcut path stays at one API request. |
| T56 | First-run copy leaked vendor names, asked for a "Username" that is the email, and hardcoded the app name (PRODUCT-8) | **done** (SignedOut, NotFound, nudge; `UpdatePrompt` and `machine.ts` are S2's) | SignedOutScreen: "Sign in with your email. The sign-in page opens in your browser; once you add a passkey there, it will offer it instead of a password."; NotFoundScreen names `config.appName`; the passkey nudge shows only once the library has a note. |
| T57 | The PWA install surface shipped the placeholder icon and a minimal manifest (PRODUCT-9) | **done** (manifest half; the icon is the owner's) | `manifest.config.ts` gains `id` (the instance's base), `categories: ['productivity']` and two `screenshots` (narrow 390×844, wide 1280×800) taken from the real build over the stub API by the opt-in `e2e/screenshots.spec.ts` (`SCREENSHOTS=1`) into `public/screenshots/*.jpg` — JPEG so the service worker's PNG precache glob leaves them alone. `manifest.test.ts` and `e2e/manifest.spec.ts` check them. |
| T63 | The bulk delete dialog used plural copy for one note (UX-17) | **done** | Archive, Restore and Delete forever dialogs branch body and label on `selectedIds.size === 1` ("It leaves your notes…", "Delete it forever"). |
| T64 | The sign-out confirmation was three sentences for the common case (UX-18) | **done** | Title "Sign out of Chintan on this device?", body "Your notes stay on the server." unless `hasUnsentWork`, which keeps its warning. |
| T22 | Every access token carried `aws.cognito.signin.user.admin`, requested in #30 for an in-app passkey list that never shipped; with the token set in `localStorage` any script that read it could `DeleteUser`, change the recovery email or strip passkeys for an hour (review 2026-09-21 r3, INFRA-3/SECURITY-1) | **done** (frontend) | `oauth.ts` asks for `openid email profile` only. Fewer scopes than the client allows is always accepted, so the app works against the pool as deployed and after S8 drops the scope from `AllowedOAuthScopes` — which must merge only after this has deployed, so no client requests a scope the pool no longer grants. Existing tokens age out within the hour. `passkeys.ts` already said the token lacks the scope. |
| T23 | Sign-out did not revoke the refresh token: `/logout` ends the hosted UI's cookie and nothing else, so a token copied off a lost or handed-over phone stayed valid for thirty days (r3, SECURITY-2) | **done** | `performSignOut` reads the refresh token before the clear, empties the device as before, then `POST <cognitoDomain>/oauth2/revoke` with `token` and `client_id` form-encoded (RFC 7009 for a public client), bounded by a 5 s `AbortSignal.timeout` and best-effort — offline, refused or timed out all still reach `/logout`. Cognito then refuses the grant and every token it minted; the id token already in a copier's hands passes the API's stateless check until it expires (≤1 h), and nothing shorter exists without a denylist. Tests: the request, the device clean before the network is asked, a failed delivery, a set with no refresh token. |
| T22 | Every access token carried `aws.cognito.signin.user.admin`, requested in #30 for an in-app passkey list that never shipped; with the token set in `localStorage` any script that read it could `DeleteUser`, change the recovery email or strip passkeys for an hour (review 2026-09-21 r3, INFRA-3/SECURITY-1) | **done** (frontend) | `oauth.ts` asks for `openid email profile` only. Fewer scopes than the client allows is always accepted, so the app works against the pool as deployed and after S8 drops the scope from `AllowedOAuthScopes` — which must merge only after this has deployed, so no client requests a scope the pool no longer grants. The access token in hand ages out within the hour, but the refresh token granted before this carries the scope for its thirty days and every refresh mints another admin-scoped access token until that grant ends; nothing in the code ends it, so the owner signs out once after this deploys (T23 now revokes the grant) and before S8's template change merges. `passkeys.ts` already said the token lacks the scope. |
| T23 | Sign-out did not revoke the refresh token: `/logout` ends the hosted UI's cookie and nothing else, so a token copied off a lost or handed-over phone stayed valid for thirty days (r3, SECURITY-2) | **done** | `performSignOut` reads the refresh token before the clear, empties the device as before, then `POST <cognitoDomain>/oauth2/revoke` with `token` and `client_id` form-encoded (RFC 7009 for a public client), sent with `keepalive` and not awaited so it outlives the `/logout` redirect on the next line — awaiting it under a timeout left a live Sign in button on screen for as long as a captive portal held the request; offline or refused still reach `/logout`. Cognito then refuses the grant and every token it minted; the id token already in a copier's hands passes the API's stateless check until it expires (≤1 h), and nothing shorter exists without a denylist. Tests: the request, the device clean before the network is asked, a failed delivery, a set with no refresh token. |
| T37 | `cacheNoteList` ran `getAll('notes')` — every cached note, bodies included — on every library mount and every `['notes']` invalidation, to compare twenty rows (r3, FRONTEND-09) | **done** | The page's keys are read in one read transaction before the write transaction opens, as the detail write already did for its one record; the no-await-inside-the-write rule still holds. A test spies on `getAll` and proves the write lands. |
| T44 | Offline, every row was tappable but an unopened note was a dead end — the unfinished half of D8 (r3, UX-16) | **done** (prefetch); **next** (dimmed rows) | `useCachedNotes` schedules an idle callback (`requestIdleCallback`, a 2 s timer on Safari) that walks the newest twenty active rows in `byUpdatedAt` and fetches in full, one at a time, those the device holds as a list row alone (`notesWithoutBody`), through `cacheNoteDetail`; once per session per version, an offline failure ends the pass, a single refusal skips that note. Vitest covers the fetch, the skip of notes held in full and of the archive, the twenty-row ceiling and the offline guard; `e2e/offline.spec.ts` opens a never-opened note offline after seeing the list. Not done: dimming uncached rows while offline with a caption — `NoteRow` and `NotesScreen` are S3's and S4's files this round; `notesWithoutBody` is the read to build it on. Seen while landing: `NoteDetailScreen` mounts from the device's copy before the server answers, and `Recordings` picks the recording that opens itself at mount, so a recording that appeared since the copy was taken without a status transition stays closed — as it already did for any note opened before on the device, and now for every first-page note; the a11y e2e seeds its capture before the first visit. If S3 wants the newest recording to open from the first server copy, the decision belongs after `served` arrives. |
| T45 | `/capture` (the Record shortcut) and `/notes/*` ran `networkFirst` and awaited GitHub Pages' 404 — 890 ms on Fast 3G, the whole 4 s timeout on a hanging link — before falling back to the very same shell that Workbox already served `/` from cache-first; INFRA-5's stale-shell window lived on the same path (r3, PERF-2 partly refuted, INFRA-5) | **done** | `sw.ts` answers every navigation with the precached shell when one exists and runs `networkFirst` only when there is none. Updates never travelled through that fetch: the browser re-checks `sw.js` on navigation, a changed build installs a new precache and `UpdatePrompt` offers it — how `/` has always updated, and how a client learns a recreated stack's config; the header comment now says so. A Chromium e2e asserts the worker makes no network fetch for a deep-linked document, and was shown to fail against the previous worker. `vite.config.ts`'s comment still says "network-first shell" — one line, left for whoever next touches that file. |
| T52 | The signed-out screen rendered `error_description` from the URL as its alert, so a composed link printed a stranger's sentence in the app's own voice (r3, SECURITY-3, reproduced in Playwright) | **done** | `describeAuthError(code)` says one of three fixed sentences (cancelled; service unavailable; could not be completed); `readCallbackParams` drops the description at the boundary; the error branch is honoured only when the `state` Cognito echoes names the flow this device started (`hasPendingFlow(state)`, a non-consuming read beside `takePending`), because Cognito redirects here only in reply to a request from here — and the pending entry is in `localStorage`, so a composed link must not end another tab's genuine sign-in either. The refusal ends the pending flow and comes off the address bar. `useAuth.test.tsx` mounts the gate with a phishing description and with no pending flow. |
| T58 | Malayalam chillu spelling variants — the atomic letter (U+0D7A–U+0D7F) and `<consonant, virama, ZWJ>` — render alike and compared unequal, so a word Whisper wrote one way was never found typed the other, in Find in note or Search (r3, LANG-6) | **done** (frontend) | One table of the six pairs in `find.ts`. `foldText` folds the sequence to the atomic letter under the consonant's index, so the match reports and highlights all three code points; a stray ZWJ/ZWNJ folds to nothing. `foldMalayalam` is the same rule by regex, without offsets, for `rankLocal`, which folds the whole corpus on every keystroke and cannot afford the per-character walk; `excerptAround` locates the hit with `findMatches` so the excerpt shows the spelling as written. Backend half (`SearchText`, `Tokenize`) is S5's. |
| T4 | Self sign-up was open on the production user pool: the managed-login page offered "Create an account" and `/signup` answered 200 | **done** | `AdminCreateUserConfig.AllowAdminCreateUserOnly: true` on `UserPool` — an in-place update (the property is Mutable in the CloudFormation resource spec, and the pool has no create-only properties). `doctor.sh` reads the flag off the deployed pool and reports `self sign-up <stack>`; README → Security says only `invite-user.sh` creates accounts. |
| T22 | Every access token carried `aws.cognito.signin.user.admin`, which nothing calls | open — PR #70, merges after the S6 frontend deploy | Scope removed from `AllowedOAuthScopes` in #70; the frontend stops requesting it first (S6), because a client asking for a scope the pool no longer grants fails sign-in with `invalid_scope`. Existing tokens age out in an hour. Flip this row to done when #70 merges. |
| T25 | No termination protection while the CI role holds `DeleteStack` | **done** (code) · owner (grants) | `protect_stack` in `scripts/lib/common.sh` enables it after every successful deploy in `deploy.sh`, `bootstrap.sh` and `setup.sh` (a warning, not a failure, when the grant is missing); the GitHub role (`bootstrap.yaml`) and the agent (`agent-policies/permissions.json`) gain `cloudformation:UpdateTerminationProtection`; `cleanup-aws.sh` and `teardown.sh` disable it first, after their typed confirmation and before anything irreversible. Owner: `setup.sh --apply` and `bootstrap-agent.sh --apply` for the grants (morning-queue §4 item 4); the instance stacks are protected by their next deploy. |
| T29 | Inviting a second user was CLI-only with no hand-over, reset or offboarding recipe | **done** (docs and script) · owner (cap) | README → Operate → **Users**: invite → hand the password over out of band → first sign-in with the email → passkey; reset; `--disable`; offboard = `chintanctl erase` then `--delete`. `invite-user.sh --disable` and `--delete` print the sub first. The shared spend cap is the owner's call (morning-queue item 10). |
| T49 | Dependabot's frontend and Go groups bundled majors with patches | **done** | `minor-patch` and `major` groups with `update-types` for gomod and bun; the actions group is unchanged. Dependabot security updates enabled through the API. |
| T50 | CI on `main` cancelled itself when two merges landed back to back | **done** | `cancel-in-progress: ${{ github.event_name == 'pull_request' }}`, and every main commit is its own concurrency group (`github.sha`): a shared group holds at most one pending run and would still cancel the middle of three. |
| T51 | `pyyaml` unpinned in both deploy workflows | **done** | `pyyaml==6.0.3`, as `ci.yaml` already had. |
| T54 | `bun audit` advisories and no frontend audit gate | **done** | `bun audit --audit-level=high` in the Frontend job, no ignore list: `fast-uri` (eslint › ajv, workbox-build › ajv) is bumped to 3.1.8 in `bun.lock`, which clears the four highs. Dependabot never sees `bun.lock` transitives, so such a bump is done by hand (the step's comment says how). |
| T55 | Export snapshots written to S3 never expired | **done** (lifecycle half) | Rule `ExpireExportSnapshots`: `tenants/` + `chintan-artifact=export`, one day, noncurrent one day. S7 sets the tag on `export.json` and `job.json`. |
| T65 | `AWS_ROLE_ARN` and `AWS_REGION` repository secrets read by nothing | **done** (code) · owner (secrets) | `setup.sh` no longer sets `AWS_REGION`; README says the region is fixed in the workflows' `env`. `gh secret delete` for both is morning-queue item 6. |
| T66 | `deploy.sh`'s replacement guard ignored a Remove of a stateful resource | **done** | The jq also selects `Action == "Remove"`; the self-test gains a bucket-removal case and keeps a stateless Remove in the harmless set. README → Operate says "replace or remove". |
| T67 | Stale "Chromium only" comment above the step that installs WebKit | **done** | Deleted. |
| SM1 | `GET /v1/notes?kind=` and `?tag=` returned a cursor whenever the raw page of rows was full, however few of them matched, so Home read "Checklists · 0+" and the chip disagreed with the filtered list (smoke 2026-09-21, finding 1) | **done** (backend) | The filter rides `repository.ListOptions.Keep` into the store's own shelf predicate — applied to the drained partition before the page is cut and the cursor minted, in both stores — so a page holds up to `limit` matches and the cursor is set only when more matches exist. No extra reads: the list already drains the partition once per page. The handler validates `tag`/`kind` before the read and post-filters nothing. `TestFilteredListPagesOverMatchesNotRows`: 60 plain + 1 checklist at limit 50 → one page of one, no cursor; 120 checklists at limit 100 → 100 then 20; 30 tagged among 90 plain → one page of 30. OpenAPI `tag`/`kind` wording, the `notesPageChecklists` fixture comment and `docs/design/checklists.md` follow. |
| SM2 | The Items tab's rows were single-line inputs and clipped a dictated sentence on a phone (finding 2) | **done** (frontend) | `ItemField`: a `<textarea rows={1}>` for every open item and the add row, sized by `field-sizing: content` with the existing `useAutoGrow` as the fallback. Enter still inserts the next item (prevented, never a newline; a pasted break becomes a space through `setItemText`/`insertItemAfter`), Backspace on an emptied item still removes it, blur saves. Rows align to the top so the box stays beside the first line; the field's block padding centres one line on the 44 px box, so a short item sits where it did. Focus after a write lands at the end of the words, where Backspace goes on editing — a script-focused textarea starts at 0. `checklist.css` tokens only; unit test for the wrapping field; `e2e/checklist.spec.ts` unchanged and green on Chromium. |
| SM3 | "Use this list" — a PATCH whose body equals `cleaned.body` — still marked the view stale and offered a pointless Regenerate (finding 3) | **done** (backend) | `UpdateNote`: a written body equal to the stored `CleanedBody`, trailing whitespace aside, clears `cleaned_stale` instead of setting it; any other body still marks it. `TestAdoptingTheCleanedViewAsTheBodyKeepsItCurrent`. |
| T35-wire | The filing row could not gate Retry on when a capture last moved | **done** (backend + OpenAPI) | `Capture.last_progress_at` (RFC 3339, null on rows from before 2026-09-05) from `CaptureIndex.LastProgressAt`, which every hand-off already stamps (`service/capture.go`, `pipeline.go`). OpenAPI `Capture`; fixtures regenerated with `last_progress_at` in the volatile-strings map; `CaptureWire.last_progress_at?: string \| null` on the frontend. |
| Q1 | A Details change (language, kind, word-for-word, auto-clean) rides the whole draft, so the PATCH carried an unchanged `body`; `CarryCaptureMarkers` treated any body as an edit and moved every marker to the end, detaching each recording from its paragraph — "Transcribe again" then appended a second copy and "Delete recording" left the words behind (QA final, finding 1) | **done** (backend) | `CarryCaptureMarkers` returns the stored body byte for byte when the edited text reads the same with markers stripped; otherwise each marker is put back ahead of its paragraph where that paragraph is still in the edited body verbatim (aligned by exact paragraph text on its own lines, one occurrence per marker) and carried at the end only when the paragraph was rewritten or deleted. `TestCarryCaptureMarkersKeepsAMarkerBesideItsUnchangedParagraph` (unchanged, one edited, deleted, moved, same sentence twice), `TestRetranscribingAfterAnUnchangedBodySaveReplacesInPlace`, `TestDeleteCaptureAfterAnUnchangedBodySaveCutsTheParagraph`. The frontend half — send `body` only when it changed — is QA-frontend's. |
| Q2 | Under a chosen language "Transcribe again" looked like it did nothing, and `capture.language` was null on the wire after an explicit request (QA final, finding 2) | **done** (backend + OpenAPI; the chip is frontend's) | Reproduced on prod with `~/r3/lang/{en,ml}.webm`: the transcription IS re-run in the requested language — `{"language":"hi"}` on the English clip came back in Devanagari, `ml` on the Malayalam clip changed the text, `en` changed it back, and segments.json's `language` follows the request — so the byte-identical run QA saw was Whisper transcribing English speech verbatim under a forced `ml`, as `model/types.go` documents. What was wrong: the `Capture` wire schema carried no language at all, so "remembered on the capture" was unobservable. `Capture.language` (`auto` or the code the transcript was asked for, a `/retranscribe` choice outranking the note's) and `Capture.language_detected` (the provider's name for what it heard) are on the wire, in OpenAPI and `CaptureWire`; fixtures regenerated. Under a chosen language the provider echoes it back, so "Heard as X" can only differ under `auto`. |
| Q4 | Ask answers cited raw `note_<id>` ids in prose, which the renderer italicised (QA final, finding 4) | **done** | The system prompt says to refer to a note by its title in the answer and keep ids to `sources`; `ask.NameNotesInProse` replaces any `note_[0-9a-f_]+` left in the answer with the packed note's title (an unknown one is removed) before the answer is stored. `TestNameNotesInProseSwapsIdsForTitles`, `TestAskTaskNamesNotesByTitleInTheStoredAnswer`. |
| Q1-r4 | The in-place alignment from Q1 re-attached a marker to its paragraph without looking at what followed, but a paragraph runs to the next marker or the end of the body — so text typed below a recording (or between two) joined its paragraph, and Delete cut it, Move carried it, Transcribe again replaced it (review 2026-09-21 r4, High). Also: a Details save with the same body still rewrote it and marked the cleaned view stale; `NameNotesInProse` doubled a title the model had bracketed after the id and left a double space for an unknown one (Lows) | **done** (backend) | `CarryCaptureMarkers` keeps a marker beside its paragraph only when the paragraph opens the body or follows a blank line and nothing but line breaks stands between its end and the next kept marker or the end of the body; walked from the end, since a demoted marker hands what follows it to the marker before — so typing below the last of two recordings carries both markers, which is the honest answer while the rule says where a paragraph starts but not where it ends (an end fence would fix that; not done here). `UpdateNote` skips the body write, snippet and stale marking when the carried body equals the stored bytes. `NameNotesInProse` matches `note_<hex>_<hex>` with an optional bracketed title (emitted once), calls an unknown id "a note", and catches an italicised `_note_…_`. Tests: eight new carry cases, `TestDeleteCaptureAfterTypingBelowTheParagraphKeepsTheTypedText`, `TestADetailsSaveWithTheSameBodyDoesNotMarkTheViewStale`, five new naming cases. |
| Q1-fe | Changing a note's language (or kind, verbatim, auto-clean) in Details sent the whole draft, body included; the server took the unchanged body as an edit, carried every recording's marker to the end, and "Transcribe again" then duplicated the paragraph while Delete recording cut nothing (QA final 2026-09-21, finding 1) | **done** (frontend half; `CarryCaptureMarkers` keeping markers in place for an unchanged body is the backend stream's) | `patchFor` sends `version` plus only the fields that differ from `EditorModel.server`, the draft the server holds at that version: a Details-only change PATCHes without `body`. `server` stays put across an offline save so the queued PATCH (which replaces the one before it) stays cumulative, and becomes theirs on Take theirs / Keep mine / Keep both so Keep mine still writes back everything of mine that differs; `rebase` carries a flushed queue's text onto the new version and leaves the cleaned-view settings alone. append_in_progress and conflict paths unchanged. `autosave.test.ts` (six cases), `useNoteEditor.test.tsx`. |
| Q4-fe | Ask answers cited raw note ids (`note_18d2…_ff6e… (Stale check mobile): "…"`) and the Markdown renderer italicised the `_…_` run (finding 4) | **done** (frontend half; the prompt/corpus shape is the backend stream's) | `thread.nameSources` replaces every `note_[0-9a-f_]{8,}` with the cited source's title, or with "a note" when nothing names it (a dropped id left `: "quote"` orphaned), keeping a name the model put in brackets once; applied in `applyRow` so the thread, its history and Save as note all read the title. `thread.test.ts`. |
| Q6 | The archive view on a phone had no visible label: h1 "Notes · 0", the pressed "Archived" chip off-screen at the far end of the row (finding 6) | **done** | The h1 reads "Archived · N" in the archive; the chip row scrolls sideways to its pressed chip (its own `scrollLeft`, via `chipScrollBy`) until the chip sits a gutter in from the edge — keyed on the row's contents (view, tag, kind, tag names, counts), since on a cold `?view=archived` the tag chips land late from IndexedDB and pushed the chip back off the screen — not `scrollIntoView`, which in Chromium moves the sequential focus navigation starting point and sent the first Tab past the skip link. `NotesScreen.test.tsx`; `archive.spec` reloads the archive on a phone with tags seeded and asserts the chip is in the viewport. |
| Q7 | The Details drawer was a content-height sheet: 715 of 915 px on a phone, over the header on a desktop (finding 7) | **done** | `.note-panel` is capped at `60dvh` with `overflow-y: auto` and `overscroll-behavior: contain` (`notes.css`); at 60 % it also stays under the header on any real desktop. `.note-panel__head` is `position: sticky` at the sheet's top, so Close stays in reach while the content scrolls. |
| Q8 | In Nocturne the destructive "Delete recording" menu item wore the accent lime, the record button's colour (finding 8) | **done** | `--color-destructive` in `tokens.css`: `accent-strong` in Ink & Paper, `ink` in Nocturne (both blocks); `.overflow-menu__item[data-destructive]` uses it with a medium weight. |
| Q9 | The Into chooser's meta line showed a checklist's raw `- [ ]` syntax (finding 9) | **done** | `TargetChooser.optionMeta` runs a checklist's snippet through `openItemsText(parseChecklist(…))`, as the Move sheet and the row do. `CaptureScreen.test.tsx`. |
| Q10 | Opening the Into chooser while recording reflowed the screen: the state line, clock and controls moved under the thumb (finding 10) | **done** | `.target-chooser__sheet` is `position: absolute` under the pill (`--z-sheet`), anchored to `.target-chooser`; since it now covers the controls, a tap beside it or Escape closes it (document listeners, as `OverflowMenu`), and Escape hands focus back to the pill. `CaptureScreen.test.tsx`. |
| Q11 | "+1 more" hid exactly one chip behind a chip of the same size (finding 11) | **done** | The fold starts only when it hides two or more; four sources show four chips. `AskPanel.test.tsx`. |
| Q12 | About's "How filing works" still said "Record into this", the control T6 removed (finding 12) | **done** | "open it and tap the microphone, which then reads *Into this note*, or pick the note on the recording screen". `AboutScreen.test.tsx`. |
| Q13 | The Archive row at the list's end read "Archive · 0" (finding 13) | **done** | The count is hidden at zero, as the chip's is. `NotesScreen.test.tsx`. |
| Q14 | Bulk delete with one note selected did not name it (finding 14) | **done** | "“<title>” and its recordings and transcripts are destroyed…", the row dialog's sentence. `NotesScreen.test.tsx`. |
| Q15 | Offline Home's caption sat on the "TODAY" heading with no gap (finding 15) | **done** | `.library > .screen__count { margin-block-end: var(--space-3) }` in `home.css`, for the offline, loading and result-count captions. |
| Q5 | The offline body prefetch (T44, #72) was a coin flip: five cold loads of Home fetched 20, 1, 1, 20 and 1 bodies, and a first-page note then answered "Not on this device" offline (QA final 2026-09-21, finding 5) | **done** | Home fires four list GETs at mount — active, archived, `kind=checklist`, the search corpus — and each write re-triggers `useCachedNotes`. When the one-row checklist page landed first the pass started on that row alone, and every re-trigger that arrived during its GET found `prefetching` set and was dropped, with nothing to start it again. A trigger during a pass now asks for one more pass (`rowsArrivedMeanwhile`), run when the current one ends; a pass that stopped early for offline or sign-out does not loop, and `attempted` keeps the extra pass to rows no pass has asked for. Vitest reproduces the race with fake IndexedDB (the first page landing while the first body is in the air; fails 1 vs 20 on the old code); `e2e/offline.spec.ts` forces the ordering in Chromium (the checklist page answers at once, the rest are slowed) and was shown to fail 3/3 against the old hook and pass 5/5 against the fix. Live-site check was not possible: the QA pass signed out and revoked the throwaway refresh token (`/oauth2/token` → 400). |

## Owner feedback 2026-09-24

| # | Item | Status | Notes |
|---|---|---|---|
| OF-B | Pin notes to the top of Home and put the pinned ones in an order (backend) | **done** (backend + OpenAPI; Home's Pinned group, the pin actions and the drag are the frontend stream's) | `NoteIndex.PinnedAt`/`PinRank` as promoted attributes written only when pinned (no backfill); wire `pinned` (required) and `pin_rank` (nullable). `PATCH {pinned}`: a new pin lands last (`rank = 1000 × count`), the fifty-first is 409 `you can pin up to fifty notes`, `false` clears. `POST /v1/notes/pins {ids}` rewrites `pin_rank` as position × 1000 over the caller's pinned, active notes named once (else 400 `every id must be one of your pinned notes`), reading each whole before the write so `search_text`/`cleaned_body` survive. Both stores order on the exported `repository.NoteOrderKey` (pinned by rank, tie → more recently pinned, then touch order); the list cursor carries that key, and a pre-tier cursor is read as an unpinned position. Archive clears the pin; restore comes back unpinned. Tests: `handler/pins_test.go`, `repository/pins_test.go`. `docs/design/pins.md`. |
| OF-A | An inbox for external devices: any recorder, watch, ring or app that can POST with one header drops audio or text into the notes (backend + docs) | **done** (backend + OpenAPI + gateway routes + README; a Devices screen in the app is a follow-up for the frontend) | Device keys: `POST /v1/devices {name}` mints `ck_<id>_<secret>` (shown once, SHA-256 stored; not idempotent so no replay record holds it), `GET` lists without the key, `DELETE` revokes (index keys dropped, 30-day TTL); ten per tenant; rows `DEVICE#<id>` with sparse GSI1 `DEVICEKEY#<id>`/`KEY`. Inbox: `POST /v1/inbox/captures` (the two-step 201), `/inbox/audio` (body ≤ 5 MiB, the API writes the object with the retention tags; the bucket notifies the worker), `/inbox/text` (1–20,000 runes, `RawKey` written, starts at `transcribed`, worker invoked; `has_audio:false`, retranscribe 409, download 404); `Authorization: Bearer ck_…` checked in `handler/inbox.go` with `crypto/subtle`, 401 `unknown device key` for every failure, per-key 200/UTC-day → 429 `this device has reached today's limit`, counter written under the row's version so a revoke is never overwritten; the spend cap and Idempotency-Key apply as for the app; three `AuthorizationType: NONE` gateway routes, declared under the OpenAPI `deviceKey` scheme (the conformance test reads that as gateway-open). `Capture.source` (`app` \| `device:<id>`) and `Capture.has_audio` on the wire; fixtures regenerated, `CaptureWire` gains both. `docs/design/inbox.md`; README → Connect a device (curl, iOS Shortcut, Android HTTP Shortcuts). Tests: `service/devices_test.go`, `handler/devices_test.go`, `handler/inbox_test.go`, `pipeline/inbox_test.go`, `repository/devices_test.go`. |
| OF-R | Review of #87 (backend stream), Medium and above plus the Lows taken | **done** | One-shot inbox audio cap 4 MiB (`MaxInboxAudioBytes`): the gateway hands a binary body to the function base64-encoded under Lambda's 6 MB request cap, so 5 MiB never reached the handler; README, `openapi.yaml`, `docs/design/inbox.md` say 4 MiB and point longer recordings at the two-step route. `Authenticate` refuses a key at its daily limit before writing (`Version` unchanged after the 201st, tested); `RevokeDevice` retries a write that loses to the counter (`TestRevokeDeviceOutlivesACounterWriteItRacedWith`). A new pin lands one step past the highest rank in use, not at count × 1000, so it lands last after an unpin (`TestANewPinLandsLastAfterAnUnpin`). Contract fixtures: `c_note_1` and the two `needs_target` rows carry an audio key (`has_audio: true`); `captureFromDevice` is the one text shape. OpenAPI: 404/409 on `POST /v1/captures` and the two inbox capture routes, 413 on `/v1/inbox/captures`, `pin_rank` required on Note, `size_bytes` minimum 0, `DeviceId` pattern `^dev_[0-9a-f]{12}$` (and `parseDeviceKey` enforces it), the content_type sentence names all seven types; conformance scenarios for every added code. `IngestAudio`/`IngestText` delete the row when the object write fails (`TestInboxIngestLeavesNoRowWhenTheObjectWriteFails`). `ApiStage.RouteSettings` throttles the three open inbox routes at 5 rps / 20 burst. |
| HOME-1 | "Hover to get a checkbox on the main notes page is not clean UX" (contract C) | **done** (frontend, #86) | The hover checkbox is gone. Press-and-hold a row starts selection with a mouse as with a finger (`useLongPress` takes every pointer's primary button; a right-click still gets the browser's menu — and the Recordings tab, which shares the hook, gains the mouse hold too, deliberately: one gesture everywhere). Each row has a ⋮ at its right — revealed on hover/focus for a fine pointer, always visible at low emphasis under a finger — with Pin/Unpin · Archive (or Restore) · Delete · Select. Named "More" and described by the title, so the title answers to the row alone. `NoteRow.tsx`, `pins.css`; `NoteRow.test.tsx`, `NotesScreen.test.tsx`, `archive.spec.ts`. |
| HOME-2 | Pin and reorder notes on Home (contract B, frontend) | **done** (frontend, #86, rebased onto #87 for the wire; merge #87 first) | A **Pinned** group above the days in `pin_rank` order (`splitPinned`); a pin glyph on the row; Pin/Unpin in the swipe tray, the row ⋮ and the note's header ⋮ (`PATCH {version, pinned}`); drag to reorder within Pinned — a grip for a mouse, hold-then-drag under a finger, arrow keys on the grip — ending in one `POST /v1/notes/pins {ids}`. Both writes are optimistic on every cached list with rollback on refusal (`usePinNote`, `useReorderPins`). The cached rows carry `pinned`/`pin_rank` as part of the wire row, and the screen splits the cached list on the same terms. `PinnedGroup.tsx`, `pins.spec.ts`, `NotesScreen.pins.test.tsx`. |
| HOME-3 | "The last recorded one is always at the bottom" on Home | **not reproduced** — see the PR body | Every write path bumps `updated_at` (worker `refreshNoteIndex` for routed and targeted appends, API `refreshNoteIndex` after a recording delete/move, `UpdateNote` for text edits); the server sorts the drained partition by `updated_at` desc (`sortNotesMostRecentlyTouchedFirst`), Home re-sorts by the same field (`groupByDay`), the IndexedDB `byUpdatedAt` index and the day groups read it too. On prod as the test user a text-only edit moved the oldest note to the top of Today on desktop and phone, and the screen order equalled `GET /v1/notes`. The one place a new recording lands last is the note body — the paragraph or checklist item is appended at the end, by design (a note reads in order). Recordings tab is newest-first; filing receipts keep the server's newest-first. Left open: the append path could not be exercised live (HOME-4). |
| HOME-4 | Prod pipeline did not consume two uploads made 2026-09-24 04:22Z and 04:25Z (test user; `POST /v1/captures` + presigned PUT, with and without `note_id`) | **closed — not a fault** | Checked read-only in AWS: the bucket holds no object for either capture id, so the agent's presigned PUTs never landed (its own environment, not the pipeline); the worker processed the owner's own capture at 04:05Z the same morning and prod logged 0 WARN/ERROR. The two `uploaded` rows expire through the usual sweep. |
| D | Recording into a note sent the person to its Recordings tab to watch the progress strip | **done** | `captureReturnPath(noteId)` is `ROUTES.note(noteId)` in every case, so the note opens on the tab it was left on (`noteRecordings` is gone). New `FilingBanner` (`features/capture/FilingBanner.tsx`) under the meta line, above the tab strip: this device's upload with its bar (`LocalUploadItem`), then the server row with `FilingStages` and the row's Retry/Dismiss (`FilingItem`, now exported), gone the moment the capture appends; candidates are `isFilingRelevant` minus appended and dismissed (`bannerCapture`). Unit tests in `FilingBanner.test.tsx`; `e2e/capture.spec.ts` follows the banner from "Uploading" to the updated text. |
| E | Push-to-talk: hold the mic to record in place; a `/talk` screen for one thought after another | **done** | `useHoldToTalk` (features/capture): press-and-hold ≥ 350 ms on the tab-bar mic starts `store.start` into the open note or a new one, `HoldOverlay` above the bar shows the live level, clock and "Release to send · slide away to cancel"; release → `stopAndSend`; > 80 px away → discard; < 600 ms of audio → discard with the one-line "Hold to talk". A tap still opens the capture screen. `/talk` (`features/talk/TalkScreen.tsx`, lazy): the target pill, one disc ≥ 40 % of the viewport (`talk.css`), the same mechanics from the first frame, Space held on a keyboard, "Sent · filing" for 1.5 s and ready again; manifest shortcut "Hold to talk — Hold, speak, release to send". The shell's recording indicator is hidden under a hold and on `/talk` (CSS `:has`). No history is touched, so system Back is untouched. Tests: `RecordButton.test.tsx`, `TalkScreen.test.tsx`, `e2e/talk.spec.ts`, holds in `e2e/capture.spec.ts`. No native Android widget for a PWA: a thin TWA/native wrapper is the path to a home-screen widget or a hardware button (owner's call). |
| F1 | Checklist boxes were the browser's own: a native square in the Items tab, the Split up preview and beside the row's title, three looks in two themes | **done** | One drawn control (`Check` in `ChecklistEditor.tsx`, styled in `checklist.css` with tokens only): the real `<input type=checkbox>` stays, stretched invisibly over its 44 px label for the role, name, keyboard and focus ring; beside it a 22 px rounded square in the icon set's 1.75 px non-scaling stroke, ticked by `PATHS.check` drawn as a stroke (`pathLength` 1, `stroke-dashoffset` 1 → 0 over `--motion-duration-base`; one frame under reduced motion). Done: the box fills ink with the tick in ground, the words in `--color-faint` under a 1 px strike in `--color-line-strong`. The row's `checklist` glyph has the same rounded corners. The Into chooser and Move sheet show a checklist's open items as words and had no box to redraw. Review r1: the Cleaned tab's Keep-it-updated switch and the two Details switches (Word for word, Checklist) now use the same drawn box (`CheckMark`), and the older `.cleaned__auto-mark` rules are gone from `shell.css`; in the Items tab the ticked row is held where it stands for `--motion-duration-base` (read from the sheet, so 1 ms under reduced motion) before it regroups, so the tick draws under the finger — a row that moved at once was mounted afresh already ticked, and a transition does not play on mount; the Split up rows are keyed by place and words, so a delete above a row remounts it instead of sliding the tick of the row that stood there. |
| F2 | The Split up preview was read-only: to tick the split version you had to press Use this list first, then go to Items | **done** | The list is live. The first tick or × sends one PATCH whose body is the split list with that change (the same edit-and-save a tick in Items is); a caption under the header says "Ticking here replaces your list with the split version." From then on the tab shows the body itself and each act is an ordinary edit; `adoptedSplits` (note id → `generated_at`) remembers the adoption across the tab switch that unmounts the panel, and the stale notice is held back while the body is the split version. Use this list and Regenerate stay; a new view (Regenerate, auto-refresh) is a new proposal. A reload forgets the adoption: the tab then shows the proposal with the stale notice, and a tick there replaces the body again, as the caption says. Review r1: Use this list is shown only until the first act (the body already is the list, and pressing it again would have put the un-ticked proposal back over the ticks made since); the rows and their × are held with the buttons while a regeneration is pending; once adopted the header reads "Your list · split up N minutes ago" and a polite live region says "Your list is now the split version." once. Known loop, left as is: with Keep it updated on, each tick's PATCH regenerates the view, the new `generated_at` un-adopts it, and a few seconds later the tab is a (re-worded) proposal again whose next tick is a whole-body replacement — honest, because the caption returns; treating the auto-refreshed view as adopted would hide the split of a recording just appended, which is what the switch is for. `ChecklistNote.test.tsx`, `ChecklistEditor.test.tsx`, `e2e/checklist.spec.ts`. |
| A-fe | An inbox for external devices — the frontend half: a place on You to mint, list and revoke device keys, recipes for the common clients, and recording rows that say which device sent them (contract A, 2026-09-24) | **done** (frontend) | `DevicesCard` on You between Passkeys and Your data: the devices by name with when each was added and last used, "Add a device" → a name → the key once in a copy box with "it will not be shown again" until Done, per-device Remove behind a confirmation, "Add" disabled at ten with the reason. Under the list, three disclosures with copy buttons — curl (one-shot `POST /v1/inbox/audio`), an iOS Shortcut (Record Audio → Get Contents of URL), Android via HTTP Shortcuts — and one sentence for watches, rings and anything else that can POST a file with one header; the address comes from `config.apiUrl`. `endpoints.ts` gains `createDevice`/`listDevices`/`deleteDevice`, `queries.ts` `useDevices`/`useCreateDevice`/`useDeleteDevice`, `schema.ts` `DeviceWire` (optional fields until the generated fixtures carry them) and `CaptureWire.source`/`has_audio`; hand-written fixtures in `pending.ts`. Recording rows: `source` starting `device:` reads "From ⟨name⟩" (the list is asked only when a row needs it; "From a device" when the key has since been removed); `has_audio:false` rows show the transcript with no player, no play disc, and no Download/Transcribe again. The recorder's three device calls and the generated device fixtures landed with R4-31, which also took the `?` off `DeviceWire` and the hand-written device constants out of `pending.ts`. The transcript loader still asks for the audio URL of a text row and tolerates the 404 (`artifacts.ts` untouched, out of this stream's files). |
| G4 | On the deployed app a ticked checklist item jumped to Done at once instead of holding while the tick drew (QA 2026-09-24) | **done** | `motionBaseMs` read `--motion-duration-base` with `parseFloat`; the source token is `220ms` but the built sheet minifies it to `.22s`, so the hold was 0.22 ms in production and 220 ms in every test. `parseMotionMs` honours `ms` and `s` (and keeps the token's own value for anything unreadable); unit-tested with both spellings. |
| H5 | A device's recording read "From ⟨device⟩" when fetched on its own but "app" on the note's page: gsi1 projects a fixed attribute list and `source` was not in it (QA 2026-09-24) | **done** | `source` is promoted to a top-level attribute on the capture row and `ListCapturesByNote` overlays the page's sources with one `BatchGetItem` (`hydrateCaptureSources`; `SOURCE` is a reserved word, so a placeholder). Adding it to the projection instead would mean rebuilding the index by hand — CloudFormation cannot update a GSI's projection — so that is queued as an owner decision and the overlay goes with it. `TestListCapturesByNoteKeepsTheSource` runs against the fake's template-derived projection and fails without the overlay. |
| QA-D | A recording whose upload never completed sat in `uploaded` for ten hours with a "Still not done" card, and `DELETE /v1/captures/{id}` answered 409 "still being processed" (QA 2026-09-24, two test-tenant rows from 04:22Z/04:25Z) | **done** | `DeleteCapture` refused every pending status outright; retry, move and re-transcribe already used `CaptureStuck` (no worker can have touched the row for 15 minutes, or the append lease has lapsed). Delete now uses the same rule, so a stuck row can be removed and its objects unlinked, and a live one is still refused. Tests: the refusal case pins the clock a minute after the row; `TestDeleteCaptureRemovesOneStuckInFlight` deletes at the stuck boundary and checks the row, the audio and the untouched body. |

## Round 4 — 2026-09-24

| # | Item | Status | Notes |
|---|---|---|---|
| R4-2 | Reordering the Pinned group under a tag or Checklists chip re-ranked only the visible subset from 0 and scrambled the whole pinned order | **done** (frontend) | `NotesScreen` passes `reorderable={!tag && !kind}`; `PinnedGroup` draws grips and lifts a held row only when the whole group is on screen, and offers no Move items otherwise. Under a filter the hold selects, as everywhere else. `NotesScreen.pins.test.tsx` (desktop: no grip, no Move items; phone: the hold selects, nothing is posted). |
| R4-3 | Pin then Unpin before the list refetch sent the stale version, was answered 409, and left the note pinned with no message | **done** (frontend half; the pin-only PATCH skipping the version check is owner Decision 6) | `usePinNote.onSuccess` writes `pinned`, `pin_rank`, `version` and `updated_at` from the answer into every cached list and the detail key; `pin.isPending` is part of `busy` in `NoteRow` and `NoteMenu`. Tests: a refused pin (409) rolls the row out of the Pinned group with the list GET held; an Unpin straight after a Pin carries the new version. |
| R4-4 | Pinned notes could not be reordered on a phone without a drag nothing announces; no assistive-tech or single-pointer path (WCAG 2.5.7) | **done** (frontend; owner to look at the menu items once shipped, R4-15) | A pinned row's ⋮ ends with "Move up" and "Move down" (absent at the group's ends), each one step through the same `commit` the grip and the drag use, so one `POST /v1/notes/pins`. `NotesScreen.pins.test.tsx`. |
| R4-11 | On a cold offline start Pin/Unpin and a drag appeared to do nothing; the PATCH fired later with whatever version the cache held | **done** (frontend) | `useOnline()` in `NoteRow`, `NoteMenu` and `PinnedGroup`: Pin/Unpin and Move up/down are disabled and the grip is disabled while offline; the swipe tray, which has no disabled state, leaves Pin out; a held row selects instead of lifting. Tests in `NotesScreen.pins.test.tsx` and `NoteRow.test.tsx`. |
| R4-12 | Home selection mode still used the browser's native checkbox while every other box in the app is drawn | **done** (frontend) | The selectable row's `<input>` takes `.checklist__box` with `<CheckMark />` beside it inside `.note-row__check` (now positioned), so the checklist's own hiding and `:checked + .checklist__mark` rules apply; `.note-row__checkbox` is gone from `shell.css`. `NoteRow.test.tsx` pins the class and the adjacency. |
| R4-13 | Desktop pinned row: focus order ran row → ⋮ (right edge) → grip (left edge) | **done** (frontend) | The grip `<button>` renders before `<NoteRow>` inside the `<li>`; `.pin-grip` is absolutely positioned so nothing moves, and a `z-index` keeps it above the row, whose positioned wrap now follows it in paint order; without that the row took every mouse press meant for the grip (caught by `pins.spec.ts` in CI). The arrow-key test asserts the DOM order. |
| R4-14 | `pins.spec.ts` ran on Chromium only | **done** (frontend e2e) | `pins` added to the WebKit `testMatch`; the "on a phone" describe is skipped on WebKit (`CDP touch`), so the mouse half runs there. Verified in CI only (WebKit does not launch on orb; orb was frozen this round). |
| R4-6 | The note screen showed two indicators for one upload: the filing banner and the shell's "Sending a recording — tap to return" pill, whose tap led to a capture screen that only bounced back | **done** (frontend) | `RecordingIndicator` also returns null while the store is `uploading` into the note whose `/notes/:id` route is open; an upload aimed at a different note still shows the pill there, where no banner does. `RecordingIndicator.test.tsx`. |
| R4-17 | The tab-bar mic hold had no visibility cancel: a hold the OS interrupted kept the microphone open until the next press, whose release sent everything recorded meanwhile | **done** (frontend) | `useHoldToTalk` ends a hold on `document.visibilitychange` to hidden as a release, not a cancel (review round 4b): what was said before the call is the message — the hook's own rule is a partial recording, never a discard — a slip under `MIN_TALK_MS` gets the hint, and an armed press stands down through the `pointercancel` path. `visibilitychange` rather than `blur`, so the permission prompt — which takes focus without hiding the page — cannot end the hold that raised it. `/talk`'s own Space-bar `blur` cancel, from an earlier round, is unchanged. `RecordButton.test.tsx`: a slip and a message, each hidden mid-hold. |
| R4-18 | `/talk` on a 1280×800 desktop put the hint — the only mention of Space — under the tab bar | **done** (frontend) | Disc cap `min(100%, 26rem, 45svh)` and the column's block padding `--space-3` (`talk.css`). Not measured live tonight (orb frozen): by the report's numbers the smaller disc recovers 56 of the 71 px overrun and the padding 24 more. |
| R4-19 | Hold-to-talk wording disagreed with itself: "Sent · filing" over an "Uploading… 10%" row; "Too short — hold to talk" on `/talk` vs "Hold to talk" in the tab-bar overlay | **done** (frontend) | The `/talk` status is read from the store, not the hold's 1.5 s notice (review round 4b: an upload longer than `HOLD_NOTICE_MS` went blank and never said Sent): "Sending…" while the model is `stopping`/`review`/`uploading`, "Sent · filing" while it holds the landed upload — on `/talk` until the next hold or `HANDOFF_GRACE_MS`, since no server row is listed there to take it earlier — and nothing after a failed upload (the row and the failure line explain); the overlay's hint is "Too short — hold to talk". `TalkScreen.test.tsx` (one case parks the POST past the notice), `RecordButton.test.tsx`, `e2e/capture.spec.ts`, `e2e/talk.spec.ts` (no longer waits for the status to clear before the next tap). |
| R4-20 | The hold overlay's bottom edge touched the "Into this note" pill on a note | **done** (frontend) | `.tab-bar:has(.hold-overlay) .tab-bar__into { visibility: hidden }` (`capture.css`): the pill steps aside while any hold card is up — holding, the too-short hint, "Still sending" — and returns with the bar unchanged. |
| R4-21 | Filing stages' accessible text read "Uploaded in progress" | **done** (frontend) | The first stage's label is "Upload" (`FilingRow.tsx` `STAGES`), so the " in progress" / " complete" suffixes read as sentences. |
| R4-22 | Hold-to-talk unit tests timed the gesture on the real clock, so a ≥150–300 ms stall on a shared runner turned a tap into a hold or a too-short hold into a message | **done** (frontend tests) | The tap, too-short and busy cases in `RecordButton.test.tsx` and the press and Space cases in `TalkScreen.test.tsx` run under `vi.useFakeTimers({ shouldAdvanceTime: true })` (`onAFakeClock`, shared from `src/test/clock.ts`) with `vi.advanceTimersByTime`, as `NotesScreen.pins.test.tsx` holds a row; the recorder's promises and `waitFor` still run, a stall moves the clock one tick. Gates ran in CI only tonight. |
| OF-M | Owner 2026-09-25: "Move recording" offers only existing notes as the target — it should also be able to start a new note; and the recording's text should move with it | **open** → follow-up PR after Round 4 (owner request; `MoveSheet.tsx` and `Recordings.tsx` — S4's file tonight, so not S3's) — the text half is **confirmed** as already so | `MoveSheet.tsx` deliberately offers no "New note" (its head comment: moving is re-filing what exists) and that comment is rewritten when the option lands, not left contradicting the UI. Adding one is a `POST /v1/notes` from the sheet followed by the existing `moveCapture` to the new id, on the Recordings tab. The text already travels, and visibly: `MoveCapture` (`backend/internal/service/capture_move.go`) cuts the recording's paragraph out of the source body and puts it into the target, or back where it was if the target write fails, and `useMoveCaptures.onSuccess` (`queries.ts`) refreshes both notes' bodies, so no reload is needed. |
| R4-5 | `POST /v1/devices` was retried by the app under an Idempotency-Key the server deliberately ignores, so a lost 201 minted up to four live keys, three never shown (client half) | **done** (frontend; the OpenAPI sentence is S1's) | `createDevice` sends once (`retry: NO_RETRY`), drops the misleading `idempotencyKey` parameter and its docstring says why the server will not replay it. `DevicesCard` shows "Could not confirm — check the list before trying again." on a timeout instead of the client's "It will be retried." and refetches the list so the sentence is true; the stale test comment is fixed. Tests: a 503 and a timed-out create each make exactly one POST (both fail against the old client). |
| R4-16 | Hold-to-talk and `/talk` had no way in from inside the app; About described the mic as tap-only | **done** | One `RowLink` "Hold to talk — Hold, speak, release to send" at the end of You → Recording & transcription, to `/talk`. About: the intro gains "— or hold the mic and let go to send —", step 1 "Tap Record and speak, or hold the microphone and release to send.", the Into this note sentence "— tap to open the recorder, or hold to talk". `SettingsScreen.test.tsx`, `AboutScreen.test.tsx`. |
| R4-25 | After Create key focus dropped to `<body>` and the key box, mounted already filled, was not announced | **done** | The `.device-key` box has `tabIndex={-1}` and is focused in an effect when `minted` is set, so a screen reader reads the sentence and the key and the next Tab is Copy key (the global `[tabindex='-1']:focus` rule draws no ring, as for a route change). Done needs nothing: Chromium's focus start point already lands on Add a device. |
| R4-26 | Done removed the key from the DOM but the mutation held it for the card's lifetime | **done** | `create.reset()` on Done and on the Add a device tap that already cleared `minted`. No DOM-observable behaviour, so no test. |
| R4-27 | Device-name placeholder was the browser's grey at 4.09:1 | **done** | `.you-input::placeholder { color: var(--color-faint); opacity: 1 }` in `you.css`, as the checklist, search and tag fields do. |
| R4-28 | Device form error wore the record-state accent | **done** | `.device-form__error` uses `--color-destructive`. |
| R4-29 | The three recipe disclosures looked like underlined links with no open state | **done** | A `chevron-right` `Icon` in each `<summary>` turning 90° under `details[open]` (`--motion-duration-fast`, so one frame under reduced motion); `.recipe > .you-card__more-summary` drops the underline, which stays for the foot's "More". |
| R4-30 | The card said "two hundred recordings a day" (the counter counts requests) and never said how to aim a device at one note | **done** | Foot: "A device may send two hundred requests a day (recordings or text), up to 4 MiB each."; under Connect a device: "To file into one note every time, add the header `X-Chintan-Note-Id` with the note's id (the last part of its address)." A Copy note id row in Details is LATER. |
| MOVE-NEW | Move a recording to a note that does not exist yet ("New note…" in the Move sheet); the text must travel with the recording (owner, 2026-09-24) | **done** (backend + OpenAPI; the sheet entry is S4's) | `CaptureMove` takes `note_id` or `new_note_title` (oneOf, the same field, trim and 1–200 runes, and the same sentences as `CaptureTarget`: `supply either note_id or new_note_title`, `… not both`, `title is required`, `title is N characters; the limit is 200`). `MoveCaptureToNewNote` checks the capture first, creates the note through the notes service as `/target` does, then runs the existing move, so the paragraph is cut from the source and inserted as the new note's first — the text was already travelling with the recording on every move; nothing about that changed. A failed move is compensated (`discardUnusedNote`): the note has a fresh id per call and a 5xx is not replayed under its Idempotency-Key, so each retry would have left one more empty note behind "nothing changed"; a new note whose body is still empty (every `ErrMoveIncomplete`) is removed through `NoteCreator.DiscardNote` — body, meta, row, no capture cascade, since a re-point that reported a fault may have landed — and one the paragraph reached is kept and logged by id at Error. Answer: the 200 `Capture` whose `note_id` is the new note, as `/target` answers. Tests: `service/capture_edit_test.go TestMoveCaptureIntoANewNote` (table: note exists, paragraph in it and out of the source, capture points at it, refusals make no note), `TestMoveCaptureIntoANewNoteCompensatesAFailure` (a failure before the cut leaves no note and the source intact; one after the insert keeps the note with the text, logged by id), `handler/captures_edit_test.go`, conformance 400 (both fields). |
| R4-1 | A pin or unpin PATCH rewrote `updated_at`, so an unpinned note re-filed under Today and a pinned row read as edited | **done** | `UpdateNote` bumps `updated_at` and rewrites the meta object only when a field other than `pinned`/`version` is set; the version still moves through `putCarryingStamp`. `TestPinningDoesNotTouchUpdatedAt`; OpenAPI `NoteUpdate.pinned` says so. |
| R4-8 | An S3 event for a capture row deleted by hand (15-minute stuck delete vs 30-minute presigned PUT) failed the worker into the DLQ and orphaned the object | **done** | `runCapture` with an S3 object key and `ErrNotFound` deletes the object, logs `object for a deleted capture; removed`, counts `CaptureOrphanObjectRemoved` and returns nil, as `RejectOversizedCapture` does for its rowless case — but only when the notification is at least 30 s old (`orphanObjectAfter`, from the record's `eventTime`, which a Lambda retry redelivers unchanged): `GetCapture` is an eventually consistent read and the inbox writes the row then the object back to back, so a first delivery that misses the row still fails the invocation and is retried, and the retry a minute later finds the row or removes the object. A direct invocation for a missing row is still the retryable fault. `orphan_object_test.go TestWorkerRemovesAnObjectWhoseCaptureWasDeleted` (first delivery refused and the object kept; the retry removes it); `TestInfrastructureFailureFailsTheInvocation` sends a stamped event, so it fails through the age gate rather than the zero-time short-circuit. |
| R4-9 | `ReorderPins` stopped at the first version conflict with the earlier ranks written and the later ones not | **done** | `putPinRank` re-reads the note and writes the rank again, `maxIndexRefreshAttempts` times; a note that keeps moving is still 409; one unpinned or archived meanwhile is 400 as the validation would have said. `TestReorderPinsRidesOutAVersionMove` (one note moves, every note moves, one keeps moving). |
| R4-10 | `inbox_test.go` repeated the two flake shapes #89 removed: a wrong secret built by overwriting four characters, and the daily counter on the real clock | **done** | The last character is flipped as #89 does; the harness's `DeviceService` runs on a fixed clock (`harnessNow`). The router counts requests on its own clock and `handler.Deps` carries none to pin, so the usage-day assertion sums the day read before the request and the next one, which a midnight between them cannot fail. |
| R4-33 | OpenAPI said a new pin's rank is 1000 × the pins so far | **done** (OpenAPI half; `pins.md` and OF-B are S7's) | `NoteUpdate.pinned`: one step (1000) past the highest rank in use, so it lands last after an unpin's gap. |
| R4-34 | OpenAPI omitted codes the routes return: 404/409 on `/v1/inbox/text`, 409/413 on `/v1/notes/pins`, 413 on `POST /v1/devices` | **done** | Declared, with a conformance scenario for each (`withConflictingNotePuts` makes every note write lose its version check for the pins 409). |
| R4-35 | OpenAPI counted five routes past the JWT authorizer; the gateway has the CORS preflight too | **done** | "(plus the CORS preflight route, `OPTIONS /{proxy+}`, which the Lambda answers 204)". |
| R4-5 | `POST /v1/devices` must not be retried by a client (the server deliberately keeps no replay record) | **done** (OpenAPI sentence; the client's `NO_RETRY` is S5's) | "Clients must not retry this request: an attempt whose 201 was lost has still issued a key, and a retry issues a second one nobody will see." |
| R4-23 | A tick on a stale, un-adopted Split up proposal replaced the whole body with a list that predated the note's later changes: the rows stayed enabled under "The note changed since this was generated", and one tick wrote the old proposal as the body in one PATCH, so an item a recording had appended since left the body silently (review r4, frontend-logic) | **done** (frontend) | `CleanedPanel`: the Split up rows and their × are held (`disabled`) while the proposal is stale and not yet adopted, under the existing stale notice with its Regenerate now; Use this list stays the explicit overwrite, and after it the rows are the body and free again. The "Ticking here replaces your list" caption is held back with the rows, since a sentence about ticking above rows that cannot be ticked contradicts itself. `ChecklistNote.test.tsx`: a stale proposal, a tick and a × write nothing, Use this list writes the proposal as it stands. |
| R4-24 | A text-only device row (`has_audio: false`) explained audio timestamps: "Cleanup changes the wording, so these lines have no reliable timestamps — there is nothing to jump to" over words that were posted as words and never had any (review r4, ux-live) | **done** (frontend) | `TranscriptPanel` takes `textOnly`; with it neither view caption is shown — the cleaned view's cleanup sentence nor the raw hint to tap a line, since a text row seeks nowhere. `Recordings.tsx` passes it for `has_audio === false` rows. `TranscriptPanel.test.tsx`, `Recordings.test.tsx`. |
| OF-M | Move a recording to a note that does not exist yet: the Move sheet offered only existing notes, so a recording found misfiled had to wait for its note to be made elsewhere first (owner, 2026-09-24) | **done** (frontend; the server half — `new_note_title` on `POST /v1/captures/{id}/move` — is the backend stream's, and this merges after it) | `MoveSheet`: a "New note…" row (a real button, the option class's 44 px) heads the list under the search, and unfolds into a "Name the new note" field (1–200 characters, the search term prefilled when it matched nothing); Enter or "Create and move" sends the move as `{new_note_title}`, Escape or Back returns to the list with focus on the row. `useMoveCaptures` takes a `CaptureMoveWire`; for a title the first move makes the note and the rest follow it by `note_id` from the answer, so several recordings land in one note. The notice is the same "Recording moved to “⟨title⟩”" with "Open ⟨title⟩" at the id the answer named; the source note, the new note and the lists are invalidated. The paragraph goes with the recording as before — that is the server's move, and the sheet says so. Tests: `MoveSheet.test.tsx` (the row, an empty name, the request shape, Escape/Back), `Recordings.test.tsx` (the POST body, the notice and offer, several into one, a refusal). |
| R4-7 | An inbox 401/429 storm had no alarm short of the monthly budget e-mail, and the 5 rps route throttle it relied on was unmonitored | **done** (infra + docs) | `ApiClientErrorsAlarm`: `AWS/ApiGateway` `4xx` Sum ≥ 1000 per 300 s on the `ApiId` dimension, `notBreaching`, → `AlarmTopic`, under `AlarmsEnabled` — the fifth alarm, inside the free ten; a thousand in five minutes is ~40× anything the app produces, so it fires only under a flood. The three inbox `RouteSettings` drop from 5 rps / 20 burst to 1 rps / 10 burst (still ~40× ten devices × 200/day; about a fifth of the worst-case bill). `docs/design/inbox.md`'s threat model gains the per-route ceiling and its trade-off (a flood at the public URL pauses the inbox for the owner's own devices; the app uses other routes; CloudFront + a WAF rate rule is the path if it ever matters). Supersedes OF-R's "5 rps / 20 burst". Gated by CI's cfn-lint 1.56.0 (no local run tonight). Not done: the optional `InboxKeyRefused` counter in `deviceAuthenticated`. |
| R4-32 | README told the reader to pull a session token out of DevTools localStorage to mint a device key, and named a field that does not exist | **done** | "Issue a key" now says You → Devices & shortcuts → Add a device shows the key once; curl with the session token stays as the headless alternative, without the localStorage recipe. |
| R4-33 | `pins.md` and OF-B stated the old pin-rank formula `1000 × count`; the code lands a new pin one step past the highest rank in use | **done** (docs half; the `openapi.yaml` sentence is S1's) | `docs/design/pins.md` says a new pin takes a rank one step (1000) past the highest in use, so it lands last even after an unpin left a gap. OF-B's "rank = 1000 × count" above is superseded by this row (earlier rows are not edited in place). |
| R4-36 | Backlog and morning-queue corrections | **done** | `morning-queue.md`'s end-of-afternoon status now reads "live at 16:00: frontend 8da7821, backend v0.5.29; by evening frontend 1848ba6 (#91) and backend v0.5.31 / 33d6b3f (#90, #92)". Corrections to rows above, recorded here rather than edited in place: OF-A's one-shot audio cap is 4 MiB, not 5 (`MaxInboxAudioBytes`, OF-R), and its "a Devices screen in the app is a follow-up" parenthetical is stale (A-fe is done); HOME-4's two stuck `uploaded` rows do not "expire through the usual sweep" — capture rows carry no `ttl` and the sweep touches only expired notes — they are removed by hand (delete on the row, QA-D) or by `chintanctl reconcile`. |
| R4-37 | Log-hygiene CI did not scan the handler package where the device key is parsed, and its forbidden list named no key identifiers | **done** | `check-log-hygiene.sh` scans `backend/internal/handler` by default and forbids `\brawKey\b`, `\braw\b`, `KeyHash` and `presented` beside the transcript identifiers; clean over all four packages today. The self-test gains a third case — a `slog` line carrying the presented key in a handler file — which the old regex let through. |
| R4-38 | The API Lambda carried four LLM/SSM env vars only the worker reads; the worker carried `ALLOWED_ORIGIN`, which only the API reads | **done** | `LLM_BASE_URL`, `LLM_MODEL`, `GROQ_API_KEY_PATH`, `LLM_API_KEY_PATH` removed from `ApiLambdaFunction` (`cmd/api` reads none and its role has no `ssm:GetParameter`); `ALLOWED_ORIGIN` removed from `WorkerLambdaFunction` (`cmd/worker` never imports `handler`). A comment beside each says why, as S34's does. |
| R4-31 | The contract gate covered neither direction of the devices and pins wire: the recorder stopped at `getExport`, so the Go replay never saw `POST /v1/notes/pins`, the pin-only PATCH or the three `/v1/devices` routes, and the generated fixtures carried no device shape, so `schema.ts` kept `DeviceWire`'s fields optional and `DevicesCard` tested against hand-written `pending.ts` constants; `ask`, `getAsk`, `purgeNotesBatch` and `retranscribeCapture` were unrecorded too | **done** | Recorder: `pinNote`, `reorderPins`, `purgeNotesBatch`, `ask`, `getAsk`, `moveCaptureToNewNote`, `retranscribeCapture` (a language, and `{}`), `createDevice`, `listDevices`, `deleteDevice`; `seededContractHarness` pins both notes and seeds `contract-device` and `contract-ask`. Guard: every method on `ChintanApi.prototype` is spied and one neither called nor named in `ALLOWED_UNRECORDED` (empty) fails the recorder. Fixtures: `deviceCreated` (`DeviceCreatedWire`, the 201 with `key`) and `devicesPage` (`Page<DeviceWire>`), with `key` and `last_used_at` stabilised. `DeviceWire` loses its `?`s and its `key`, which moves to `DeviceCreatedWire` — a list item never has one, so one type could not be strict about both. `DevicesCard.test.tsx` reads `responses.ts`, with the ids made distinct because the generator collapses them to one stand-in and the fake server removes by id. A-fe's "Not done" clause is done. |
| R4-31b | `openapi.yaml` was not strict YAML: `Present only with `reason: append_in_progress`.` has `: ` inside a plain scalar, which PyYAML refuses ("mapping values are not allowed here") and Go's parser lets through | **done** | The scalar is single-quoted; `python3 -c "import yaml; yaml.safe_load(open('docs/api/openapi.yaml'))"` passes. |

## Owner feedback 2026-09-26

| # | Item | Status | Notes |
|---|---|---|---|
| OF-RING | The Pebble Index 01 ring's webhook got 401 in 0 ms from `POST /v1/inbox/audio` (2026-09-26 14:21Z): the ring's header setting is a bare key/value, so the key arrived without `Bearer `, and its body is a `multipart/form-data` form (`audio` as `audio/mp4`, `transcription`, `recordedAt`, `client`), which the raw route would have filed as a recording of type `multipart/form-data` | **done** (backend + OpenAPI + `docs/design/inbox.md`; the Devices-card recipe and README lines are the frontend PR) | `deviceKeyOf` (`inbox.go`) takes the one credential in three spellings — `Authorization: Bearer ck_…`, bare `Authorization: ck_…`, `X-Device-Key: ck_…` (read first) — with the same check and the same fixed 401; the threat model is unchanged. `inboxAudioForm`: the form is read with `r.MultipartReader()` under the raw body's 4 MiB cap, envelope included; the `audio` part (its own type; none is the raw path's 400, since RFC 7578 makes an untyped part `text/plain`) is ingested as a raw body is, a `transcription` part alone takes the text route (`acceptInboxText`, `has_audio:false`), both together files the audio and drops the ring's text (the pipeline transcribes with the note's language); `recordedAt` is ignored because a capture has no recorded-at field, `client` names nothing acted on. New fixed 400 `the form has no audio or transcription part`. `inbox_test.go`: the three spellings, the four form shapes, empty form, not-audio part, oversize (413); the conformance 400 scenario walks all four roads. Adversarial review (2026-09-26): retry semantics documented (no `Idempotency-Key` → a second capture; with one, the raw body's fingerprint means a re-encoded form is 409), `X-Device-Key` scoped to non-browser clients (not in the CORS allow-list), a `transcription` part's invalid UTF-8 becomes U+FFFD as on the JSON route, and `X-Chintan-Language`/`-Duration-Ms` are declared ignored on a transcription-only form. |
| OF-DEL | Owner 2026-09-26: "when I delete, I have to type delete. I don't like that UX" — every permanent delete was gated by typing the word | **done** (frontend) | No typed word anywhere. Delete on Home (row ⋮, swipe tray, note header ⋮, selection bar) is the archive on the tap, no dialog, with a shell toast "Deleted · kept in Archive for 30 days" and an Undo button for six seconds (`components/Toast.tsx`, a row above the tab bar like the update prompt; polite live region, real button, so keyboard-reachable); the separate Archive item is gone from Home. In the archive, Delete forever (row, header, bar) is a plain `ConfirmDialog` — the same sentence, one destructive button, focus on Cancel, no field — and a bulk Delete forever of more than ten notes is "Hold to delete N notes", a one-second press under a finger or a mouse (`holdMs`; Enter, Space and an assistive technology's click confirm at once — a held keystroke is a WCAG 2.1.1 timing; fill animation in `--color-destructive`, visible in both themes, whole at once under reduced motion). Undo re-pins a note that was pinned (`useUndoDelete`), since the server's restore comes back unpinned; the toast's timer pauses under a pointer or focus, and its Undo ring is ground on the ink card. Recording deletes are a plain confirm. `requireText`/`requireLabel` and the `useBulkDeleteNotes` archive-then-purge hook are removed as dead code. About's data paragraph and `docs/design/home.md` (new "Deleting" section) describe the flow. Tests: `ConfirmDialog.test.tsx` (plain, hold, early release, keyboard hold), `Toast.test.tsx`, `NoteRow.test.tsx`, `NotesScreen.test.tsx` (Undo returns the row; > 10 needs the hold), `Recordings.test.tsx`; e2e `archive.spec.ts` (Undo by keyboard), `recordings.spec.ts`, `pins.spec.ts`, `swipe.spec.ts`. |
| OF-DEL-2 | Live QA 2026-09-26: once Undo had been used, no later toast ever closed on its own | **done** (frontend) | `Toast.tsx` kept `attended` (the pause under a pointer or focus) in the region component, so it outlived the card: Undo unmounts the card inside the click, no blur or pointerleave fires, and the next notice mounted with the clock already stopped. The card is now its own `Card` component and the flag lives and dies with it; a notice that replaces a showing one keeps the same card and its attention. `Toast.test.tsx`: focus Undo, the clock is paused; click Undo; a second notice is gone after `TOAST_MS`. |
| OF-TEXTID | Live QA 2026-09-26: `POST /v1/inbox/text` ignored `X-Chintan-Note-Id` — the audio route read it, the text route did not, and the router auto-filed the text into a new note although the Devices card and the README promise the header for both | **done** (backend + OpenAPI + `docs/design/inbox.md`) | `acceptInboxText` (`inbox.go`), the tail the JSON route and a form's transcription-only road share, takes the header when the request carries no `note_id`: body wins when both are sent, since the header exists for a client that can set headers but cannot build the JSON. Same tenant-scoped `GetNote`, so another tenant's id and a missing one are the same fixed 404 `no such resource`. OpenAPI: `X-Chintan-Note-Id` hoisted to `components/parameters/InboxNoteId`, referenced from both one-shot routes, precedence stated on `/v1/inbox/text`. `inbox_test.go` `TestInboxTextTakesTheNoteIdHeader`: header alone, body + header (body wins), missing and another tenant's id (same 404 detail); the ring form's transcription-only case already pins the header. |
| C7 | Checklist creation and cleanup pipeline: "create a shopping list and add chickpeas and green gram into it" became one item `Add chickpeas and green gram into it.` and Split up added `- [x] Make a list.`; "Add umbrella to shopping list" became the item `list` (owner, 2026-09-26; "not device-specific") | **done** (backend) | Root: nothing between router and append knew what an item is — the router deletes filing/naming words, the cleanup prompt cleans prose, `checklistItem` rendered one line, the tasks prompt was shape-checked only. Fix: one extraction call replaces the transcript cleanup for a non-verbatim checklist (`Pipeline.extractItems`, `cleanup.ItemsPrompt`/`ParseItems`, `provider.LLM.Items`) over the **raw** transcript with the list title — `{"items":[…]}`, noun phrases, app words dropped, "X and Y" split, `{"items":[]}` → `no_content` (instruction-only speech adds nothing), title-equal item dropped, >100 items or no list → the recording as one item + `ChecklistItemsDiscarded`. The router's spans and the targeted strip are not applied to checklist content (no router call on the targeted path). `checklistItems` renders N lines under the one marker; `keepTick` carries ticks line for line. Tasks mode: `NoteOutput(mode, raw, body)` refuses an answer whose `- [x]` lines are not the body's, drops an open item whose words are not in the body (`TasksItemsDropped`), and the prompt says a noun-phrase item stays as written. Live eval harness `provider.TestLiveChecklistItems` (skipped unless `LIVE_LLM=1`). `docs/design/checklists.md` rewritten for the append rule and tasks mode. Not built: applying "remove X"/"make that two" to the list (returned as one item as spoken; future `{"add","remove","done"}`), dedupe on append, router span guard for plain notes, `X-Chintan-Note-Id` on `/v1/inbox/text`. |
| C7 | Checklist creation and cleanup pipeline: "create a shopping list and add chickpeas and green gram into it" became one item `Add chickpeas and green gram into it.` and Split up added `- [x] Make a list.`; "Add umbrella to shopping list" became the item `list` (owner, 2026-09-26; "not device-specific") | **done** (backend) | Root: nothing between router and append knew what an item is — the router deletes filing/naming words, the cleanup prompt cleans prose, `checklistItem` rendered one line, the tasks prompt was shape-checked only. Fix: one extraction call replaces the transcript cleanup for a non-verbatim checklist (`Pipeline.extractItems`, `cleanup.ItemsPrompt`/`ParseItems`, `provider.LLM.Items`) over the **raw** transcript with the list title — `{"items":[…]}`, noun phrases, app words dropped, "X and Y" split, `{"items":[]}` → `no_content` (instruction-only speech adds nothing), >100 items, no list or an empty completion → the recording as one item + `ChecklistItemsDiscarded`; the prompt is the only guard on the words (no title or subsequence rule: visible beats lost), so the live eval is the check. The router's spans and the targeted strip are not applied to checklist content (no router call on the targeted path); a verbatim checklist takes the raw transcript as one item on both paths. `checklistItems` renders N lines under the one marker; `keepTick` carries ticks line for line. Tasks mode: `NoteOutput(mode, raw, body)` refuses an answer whose `- [x]` lines are not the body's, drops an open item whose words are not in the body (`TasksItemsDropped`), and the prompt says a noun-phrase item stays as written. Live eval harness `provider.TestLiveChecklistItems` (skipped unless `LIVE_LLM=1`). `docs/design/checklists.md` rewritten for the append rule and tasks mode. Not built: applying "remove X"/"make that two" to the list (returned as one item as spoken; future `{"add","remove","done"}`), dedupe on append, router span guard for plain notes, `X-Chintan-Note-Id` on `/v1/inbox/text`. |

## Round 5 — 2026-09-26

| # | Item | Status | Notes |
|---|---|---|---|
| CH-11 | `backend/internal/pipeline/pipeline.go` was 2,174 lines and 51 functions, and three round-5 streams (prompts, webhook ops, backend seams) would each have edited it | **done** (pure move, wave 0) | Six files in the same package, no export, signature or behaviour change. `pipeline.go` keeps `New`, `Run`, `RunUpload`, `runCapture`, `RejectOversizedCapture`, `run`, `wantsNoteLanguage`, the shared config consts and the two delivery sentinels; `transcribe.go`, `route.go` (with `stripInstructions`, the routing-priced call), `clean.go`, `append.go` and `status.go` take their stage's functions with their doc comments and the consts/vars only they use (`orphanObjectAfter` stays with `runCapture`, `routeOutputTokensEstimate` goes with `routeOnce`, `appendStampWait`/`appendStampPoll` with the stamp, `ErrProviderKeyRejected`/`captureProviderFailed` with `handleProviderError`). Proof: the 51 `func` lines and `go doc -all` are identical before and after; a sorted line-multiset diff of the old file against the six new ones differs only by the dropped "Small helpers" banner; `go doc -all -u` differs only where `estimateTokens`'s doc comment, which had sat above `routeOutputTokensEstimate` with no blank line between them, is back on its function. `checklists.md` and `append-vs-autosave.md` name the new files. From here the prompts stream edits `route.go`, the webhook stream `pipeline.go` (`runCapture`) and `status.go` (`persist`), the backend-seams stream `append.go`. H7 above ("explicit stage objects") is not this: the stages stay functions. |
| DN-1 | The template could only spell the Pages form of the Cognito callback URL, and nothing in it could front the API with a custom hostname | **done** (infra) | `SiteBaseUrl` (AllowedPattern `^https://[^/]+(/[^/]+)*$`) replaces `PagesHost`+`RepoName`; the callback and logout URLs stay byte-identical (`https://<owner>.github.io/chintan/dev/`). `ApiHost` and `DnsZoneId` default to `''`; `ApiCertificate` (regional ACM, DNS validation, Project tag), `ApiDomainName` (REGIONAL, TLS 1.2), `ApiMapping` (`$default`) carry `HasApiHost`, `ApiAliasRecord` carries `HasDnsZone`. `ApiEndpoint` flips to `https://<ApiHost>` when set; `ApiGatewayEndpoint` is the execute-api URL unconditionally; `ApiDomainTarget` is the CNAME target. `CfnDeployRole` gains `/domainnames`, ACM (delete gated on `aws:ResourceTag/Project`) and Route 53 (any zone — ponytail note). `scripts/check-custom-domain-dormant.sh` (+ `--self-test`) runs in CI's cfn-lint job. Dormant: today's configs create no `Api*` domain resource; staging's change set is the proof. OD-1/2/3 as decided: registrar DNS by default, Cognito domain unchanged, `/dev/` kept. |
| DN-2 | No config field could name a custom API or app host, and `ci-deploy-stack.sh` hard-wired the Pages base | **done** (infra) | `api_host`, `app_host`, `dns_zone_id` in `list-instances.sh` (bare-hostname regex; `dns_zone_id` without `api_host` refused; one `app_host` across every config or none; `--app-host`); `ApiHost`/`DnsZoneId` reach the stack through the existing parameter list. `ci-deploy-stack.sh` computes `SITE_BASE_URL` and the CORS origin from `APP_HOST` or the Pages pair; `deploy-backend.yaml` passes `APP_HOST`; `bootstrap.sh --site-base`; `setup.sh` sets the Pages custom domain via `gh api` and prints the CNAME. `--format text` unchanged. |
| DN-3 | The site build assumed the repository-name prefix and the 404 redirect kept two segments regardless | **done** (infra) | `ci-build-site.sh` builds for the root when `app_host` is set and computes `pathSegmentsToKeep` from `PAGES_BASE` (2 on Pages, 1 on a custom domain); `VITE_API_URL` follows `ApiEndpoint` with no new variable. |
| DN-4 | The smoke test would have hit the custom domain before DNS could resolve, and blamed the Lambda | **done** (infra) | `deploy.sh` smokes `ApiGatewayEndpoint` first (Lambda proof), then `ApiEndpoint` with `--retry 6 --retry-delay 10` when it differs, failing with the exact CNAME to create. |
| DN-5 | The README did not say a custom domain was possible, what it costs, or in what order to do it | **done** (docs) | README: three field rows, a `### Custom domain` subsection (why Cognito stays on amazoncognito.com, $0 on AWS, Pages → API → reinstall the PWA, the two role/bootstrap notes), the CORS bullet, the `API=` comment. |
| CH-1 | The Lambda zips carried symbol and DWARF tables nothing reads | **done** (infra) | `build-lambda.sh` adds `-ldflags="-s -w"`; api zip 8.34 MB → 4.27 MB measured. Staging's `/v1/health/ready` smoke is the behavioural check. |
| SEC-1 | An inbox refused at 25/15 min had no alarm between one typo and the 1000-in-5-min flood | **done** (template half) | `InboxKeyRefusedAlarm`: `Chintan/InboxKeyRefused` rollup, Sum > 24 per 900 s, `notBreaching`, under `AlarmsEnabled`; the sixth alarm, inside the free ten for one environment. S5 emits the metric from `deviceAuthenticated`; until then the alarm reads healthy. README/template alarm counts corrected (they said three-or-four since before R4-7). |
